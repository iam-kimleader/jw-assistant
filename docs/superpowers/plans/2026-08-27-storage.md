# 자료 보관 1차 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 만든 자료를 서버에 남겨 화면을 나갔다 와도 그대로 있게 하고, 주간 답변은 회중이 나눠 쓰게 한다.

**Architecture:** 경로 만들기와 캐시 판단을 `src/` 의 순수 `.mjs` 모듈로 떼어 `node --test` 로 덮는다. 저장소와 OpenAI 는 주입해서 시험에서 갈아 끼운다. Vercel 함수와 로컬 개발 서버 두 곳이 같은 서비스 함수를 부른다.

**Tech Stack:** Node 24 ESM · `node --test` · `@vercel/blob` · React 19 + Vite + shadcn/ui(Base UI)

**Spec:** `docs/superpowers/specs/2026-08-27-storage-and-mypage-design.md`

## Global Constraints

- Node 24 ESM. `node --test` 로 시험한다. 새 npm 의존성을 넣지 않는다.
- 폴더 이름은 영문, 파일 이름과 내용은 한국어로 쓴다.
- 새 소스 파일의 첫 줄은 역할을 설명하는 한국어 한 줄 주석이다. 설정 파일은 예외다.
- 한국어 문장은 마침표로 끝낸다. 문장 끝에 콜론을 쓰지 않는다. 코드·키값·라벨의 콜론은 괜찮다.
- 비밀값을 `.env.example` 에 넣지 않는다. `.env` 를 열거나 출력하거나 스테이징하지 않는다.
- 카카오와 Blob 은 시험에서 실제로 부르지 않는다. 주입해서 갈아 끼운다.
- **`api/*.js` 와 `src/web-server.mjs` 는 같은 서비스 함수를 부른다. 한쪽에만 들어간 변경은 결함이다.**
- **저장 실패로 화면을 막지 않는다.** OpenAI 호출이 끝난 뒤라 돈이 이미 나갔다.
- `prod-sample.md` 에는 이 작업과 무관한 변경이 커밋 안 된 채 있다. 건드리지 않는다. `git add -A` 나 `git add .` 를 쓰지 않는다.

## 이 계획이 다루지 않는 것

설계 11절의 마이페이지 화면(보관함·설정 탭)은 2차이며 별도 계획이다. 이 계획은
설계 10절의 1~5단계만 다룬다.

## 미리 확인해 둔 사실

계획을 쓰면서 실제로 돌려 확인한 것들이다. 구현자가 다시 확인할 필요가 없다.

- `weekStart(Date)` 는 아무 날짜나 그 주 월요일 문자열로 바꾼다. `buildWeekOptions()` 의
  `value` 도 같은 함수로 만들어진 월요일이다
- `@vercel/blob@2.8.0` 은 `get`·`put`·`list`·`del` 을 내보내고 `access: 'private'` 과
  `allowOverwrite` 를 받는다
- `web/src/components/ui/` 에 `alert-dialog.tsx` 가 **없다.** 과제 7에서 받아야 한다
- `src/store.mjs` 의 `저장소만들기(blob)` 는 `{읽기, 쓰기, 목록, 지우기}` 를 준다.
  `읽기` 는 없거나 판이 다르거나 던지면 `null` 을 준다. `쓰기` 는 `{판: 1, ...내용}` 으로 쓴다
- `src/auth-service.mjs` 에 `사용자경로(회원번호)` 가 있고 `/^[0-9]+$/` 로 모양을 검사한다

## 파일 구조

**새로 만드는 것**

| 파일 | 책임 |
|---|---|
| `src/storage-paths.mjs` | 저장 경로를 만들고 모양을 검사한다. 순수 함수 |
| `src/storage-paths.test.mjs` | 위 시험 |
| `src/week-cache.mjs` | 주간 자료의 캐시 판단. 있으면 안 만들고, 없으면 만들고 저장 |
| `src/week-cache.test.mjs` | 가짜 저장소·가짜 만들기를 주입해 시험 |
| `src/talk-store.mjs` | 연설 자료 저장·복원. 배정 제목이 다르면 없는 것으로 본다 |
| `src/talk-store.test.mjs` | 위 시험 |
| `api/my-profile.js` | 설정 저장 어댑터 |
| `api/my-talk.js` | 연설 자료 복원 어댑터 |
| `web/src/components/ui/alert-dialog.tsx` | shadcn 에서 받는다 |
| `web/src/components/SavedNotice.tsx` | 저장된 자료 안내 띠와 다시 만들기 |

**고치는 것**

| 파일 | 무엇을 |
|---|---|
| `src/auth-service.mjs` | `사용자경로` 를 `storage-paths` 에 위임한다 |
| `api/watchtower.js`·`api/life-ministry.js` | 캐시를 태우고 POST 로 다시 만들기를 받는다 |
| `api/options.js` | 응답에 `설정` 을 싣는다 |
| `api/talk-outline.js`·`api/talk-draft.js` | 만든 뒤 저장한다 |
| `src/web-server.mjs` | 위 다섯과 같은 변경 + 새 두 경로 |
| `web/src/lib/api.ts` | 다시 만들기와 설정 저장 호출을 더한다 |
| `web/src/lib/talk-api.ts` | 연설 복원 호출을 더하고 요청에 주간을 싣는다 |
| `web/src/lib/use-weeks.ts` | `use설정` 훅을 더한다 |
| `web/src/routes/StudyPrep.tsx` | 안내 띠와 다시 만들기를 붙인다 |
| `web/src/routes/Talk.tsx` | 설정을 서버에서 받고 저장된 연설을 복원한다 |
| `web/src/lib/talk-profile-store.ts` | 지운다. 설정이 서버로 갔다 |

---

### Task 1: 저장 경로와 모양 검사

**Files:**
- Create: `src/storage-paths.mjs`
- Test: `src/storage-paths.test.mjs`
- Modify: `src/auth-service.mjs`

**Interfaces:**
- Consumes: `weekStart` from `src/wol-week.mjs`
- Produces:
  - `주간열쇠(날짜: string) -> string` — 그 주 월요일. 모양이 아니면 던진다
  - `주간경로(종류: 'watchtower'|'life-ministry', 날짜: string) -> string`
  - `설정경로(회원번호: string) -> string`
  - `연설경로(회원번호: string, 날짜: string, 배정번호: number) -> string`

- [ ] **Step 1: 실패하는 시험을 쓴다**

`src/storage-paths.test.mjs`

```js
// 저장 경로 만들기와 모양 검사를 검증하는 테스트
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { 주간열쇠, 주간경로, 설정경로, 연설경로 } from './storage-paths.mjs';

test('같은 주의 어느 날을 넣어도 한 열쇠로 모인다', () => {
  for (const 날 of ['2026-08-24', '2026-08-27', '2026-08-30']) {
    assert.equal(주간열쇠(날), '2026-08-24');
  }
  assert.equal(주간열쇠('2026-08-31'), '2026-08-31');
});

test('날짜 모양이 아니면 던진다', () => {
  for (const 값 of [null, undefined, '', '2026-8-24', '20260824', '오늘', '2026-08-24T00:00:00Z']) {
    assert.throws(() => 주간열쇠(값), /날짜/);
  }
});

test('달력에 없는 날짜는 던진다', () => {
  assert.throws(() => 주간열쇠('2026-13-01'), /날짜/);
  assert.throws(() => 주간열쇠('2026-02-30'), /날짜/);
});

test('주간 경로는 종류별로 갈린다', () => {
  assert.equal(주간경로('watchtower', '2026-08-27'), 'weeks/watchtower/2026-08-24.json');
  assert.equal(주간경로('life-ministry', '2026-08-27'), 'weeks/life-ministry/2026-08-24.json');
});

test('모르는 종류는 던진다', () => {
  assert.throws(() => 주간경로('마음대로', '2026-08-24'), /종류/);
  assert.throws(() => 주간경로('../etc', '2026-08-24'), /종류/);
});

test('설정 경로를 만든다', () => {
  assert.equal(설정경로('777'), 'users/777/profile.json');
});

test('연설 경로를 만든다', () => {
  assert.equal(연설경로('777', '2026-08-27', 3), 'users/777/talks/2026-08-24-3.json');
});

test('회원번호가 숫자 모양이 아니면 던진다', () => {
  for (const 값 of ['777/../888', '../etc', '', 'abc', '7 7']) {
    assert.throws(() => 설정경로(값), /회원번호/);
    assert.throws(() => 연설경로(값, '2026-08-24', 0), /회원번호/);
  }
});

test('배정번호가 정수가 아니면 던진다', () => {
  for (const 값 of ['0', 1.5, -1, NaN, null, undefined, '../x']) {
    assert.throws(() => 연설경로('777', '2026-08-24', 값), /배정번호/);
  }
});

test('배정번호는 한 주 배정 수를 넘지 않는다', () => {
  assert.equal(연설경로('777', '2026-08-24', 0), 'users/777/talks/2026-08-24-0.json');
  assert.throws(() => 연설경로('777', '2026-08-24', 100), /배정번호/);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --test --no-warnings src/storage-paths.test.mjs`
Expected: FAIL — `Cannot find module './storage-paths.mjs'`

- [ ] **Step 3: 최소 구현을 쓴다**

`src/storage-paths.mjs`

```js
// 저장소 경로를 만든다. 브라우저가 보낸 글자가 경로가 되지 않도록 여기서 모양을 막는다.
import { weekStart } from './wol-week.mjs';

const 종류목록 = new Set(['watchtower', 'life-ministry']);
// 한 주 배정이 이보다 많을 일이 없다. 터무니없는 값을 여기서 자른다.
const 배정번호상한 = 99;

export function 주간열쇠(날짜) {
  if (typeof 날짜 !== 'string' || !/^\d{4}-\d{2}-\d{2}$/.test(날짜)) {
    throw new Error('날짜는 YYYY-MM-DD 모양이어야 합니다.');
  }
  const 시각 = new Date(`${날짜}T00:00:00Z`);
  // 2026-02-30 같은 값은 Date 가 다른 날로 넘겨 버린다. 되돌려 비교해야 걸러진다.
  if (Number.isNaN(시각.getTime()) || 시각.toISOString().slice(0, 10) !== 날짜) {
    throw new Error('달력에 없는 날짜입니다.');
  }
  return weekStart(시각);
}

export function 주간경로(종류, 날짜) {
  if (!종류목록.has(종류)) throw new Error('모르는 자료 종류입니다.');
  return `weeks/${종류}/${주간열쇠(날짜)}.json`;
}

function 검사한회원번호(회원번호) {
  if (!/^[0-9]+$/.test(String(회원번호))) throw new Error('회원번호가 숫자 모양이 아닙니다.');
  return String(회원번호);
}

export function 설정경로(회원번호) {
  return `users/${검사한회원번호(회원번호)}/profile.json`;
}

export function 연설경로(회원번호, 날짜, 배정번호) {
  if (!Number.isInteger(배정번호) || 배정번호 < 0 || 배정번호 > 배정번호상한) {
    throw new Error('배정번호가 올바르지 않습니다.');
  }
  return `users/${검사한회원번호(회원번호)}/talks/${주간열쇠(날짜)}-${배정번호}.json`;
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --test --no-warnings src/storage-paths.test.mjs`
Expected: PASS, 10 tests

- [ ] **Step 5: `auth-service` 가 같은 함수를 쓰게 한다**

`src/auth-service.mjs` 의 맨 위 `사용자경로` 정의를 지우고 `설정경로` 를 쓴다.
경로 만드는 규칙이 두 곳에 있으면 한쪽만 고치는 사고가 난다.

파일 첫 줄 아래에 import 를 더한다.

```js
import { 설정경로 } from './storage-paths.mjs';
```

그리고 기존의 지역 `사용자경로` 정의를 지우고, `인증만들기` 가 돌려주는 객체의
`사용자경로` 를 `설정경로` 로 바꾼다. 바깥에 보이는 이름 `사용자경로` 는 그대로
둔다. 기존 시험이 그 이름을 쓴다.

- [ ] **Step 6: 전체 시험을 돌린다**

Run: `npm test`
Expected: 전부 통과. `auth-service.test.mjs` 의 `사용자 경로를 만든다` 와
`회원번호가 숫자 모양이 아니면 사용자경로가 거부한다` 가 그대로 통과해야 한다.
통과하지 않으면 이름이나 던지는 조건이 달라진 것이니 코드를 맞춘다. **시험을
약하게 고치지 않는다.**

- [ ] **Step 7: 커밋한다**

```bash
git add src/storage-paths.mjs src/storage-paths.test.mjs src/auth-service.mjs
git commit -m "저장 경로를 만들고 모양을 검사하는 모듈을 만든다"
```

---

### Task 2: 주간 자료 캐시 판단

**Files:**
- Create: `src/week-cache.mjs`
- Test: `src/week-cache.test.mjs`

**Interfaces:**
- Consumes: `주간열쇠`, `주간경로` (Task 1)
- Produces:
  - `주간자료가져오기({종류, 날짜, 저장소, 만들기, 다시만들기?, 지금?}) -> {자료, 만든때, 새로만듦}`
    - `저장소` 는 `src/store.mjs` 의 `저장소만들기(blob)` 결과다
    - `만들기(주간)` 은 정규화된 주간 문자열을 받아 자료를 돌려주는 async 함수다

- [ ] **Step 1: 실패하는 시험을 쓴다**

`src/week-cache.test.mjs`

```js
// 주간 자료의 캐시 판단을 검증하는 테스트
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { 주간자료가져오기 } from './week-cache.mjs';

function 가짜저장소(초기 = {}) {
  const 담긴것 = new Map(Object.entries(초기));
  return {
    담긴것,
    async 읽기(경로) { return 담긴것.get(경로) ?? null; },
    async 쓰기(경로, 내용) { 담긴것.set(경로, 내용); },
    async 목록() { return [...담긴것.keys()]; },
    async 지우기(경로) { 담긴것.delete(경로); },
  };
}

function 세는만들기(값 = { title: '새로 만든 것' }) {
  const 부른것 = [];
  const 만들기 = async 주간 => {
    부른것.push(주간);
    return 값;
  };
  return { 만들기, 부른것 };
}

const 지금 = () => 1_800_000_000_000;

test('저장된 게 없으면 만들고 저장한다', async () => {
  const 저장소 = 가짜저장소();
  const { 만들기, 부른것 } = 세는만들기();
  const 결과 = await 주간자료가져오기({
    종류: 'watchtower', 날짜: '2026-08-27', 저장소, 만들기, 지금,
  });

  assert.deepEqual(결과.자료, { title: '새로 만든 것' });
  assert.equal(결과.새로만듦, true);
  assert.equal(결과.만든때, new Date(지금()).toISOString());
  // 만들기 는 화면이 보낸 날짜가 아니라 정규화된 주간을 받아야 한다.
  assert.deepEqual(부른것, ['2026-08-24']);
  assert.deepEqual(저장소.담긴것.get('weeks/watchtower/2026-08-24.json'), {
    자료: { title: '새로 만든 것' },
    만든때: new Date(지금()).toISOString(),
  });
});

test('저장된 게 있으면 만들지 않는다', async () => {
  const 저장소 = 가짜저장소({
    'weeks/watchtower/2026-08-24.json': { 판: 1, 자료: { title: '옛것' }, 만든때: '2026-08-25T00:00:00.000Z' },
  });
  const { 만들기, 부른것 } = 세는만들기();
  const 결과 = await 주간자료가져오기({
    종류: 'watchtower', 날짜: '2026-08-27', 저장소, 만들기, 지금,
  });

  assert.deepEqual(결과.자료, { title: '옛것' });
  assert.equal(결과.새로만듦, false);
  assert.equal(결과.만든때, '2026-08-25T00:00:00.000Z');
  assert.deepEqual(부른것, []);
});

test('같은 주의 다른 날짜도 저장된 것을 찾는다', async () => {
  const 저장소 = 가짜저장소({
    'weeks/watchtower/2026-08-24.json': { 판: 1, 자료: { title: '옛것' }, 만든때: '2026-08-25T00:00:00.000Z' },
  });
  const { 만들기, 부른것 } = 세는만들기();
  const 결과 = await 주간자료가져오기({
    종류: 'watchtower', 날짜: '2026-08-30', 저장소, 만들기, 지금,
  });
  assert.equal(결과.새로만듦, false);
  assert.deepEqual(부른것, []);
});

test('다시 만들기는 저장된 게 있어도 새로 만든다', async () => {
  const 저장소 = 가짜저장소({
    'weeks/watchtower/2026-08-24.json': { 판: 1, 자료: { title: '옛것' }, 만든때: '2026-08-25T00:00:00.000Z' },
  });
  const { 만들기, 부른것 } = 세는만들기({ title: '다시 만든 것' });
  const 결과 = await 주간자료가져오기({
    종류: 'watchtower', 날짜: '2026-08-27', 저장소, 만들기, 다시만들기: true, 지금,
  });

  assert.deepEqual(결과.자료, { title: '다시 만든 것' });
  assert.equal(결과.새로만듦, true);
  assert.deepEqual(부른것, ['2026-08-24']);
  assert.deepEqual(저장소.담긴것.get('weeks/watchtower/2026-08-24.json').자료, { title: '다시 만든 것' });
});

test('종류가 갈리면 서로 덮지 않는다', async () => {
  const 저장소 = 가짜저장소();
  await 주간자료가져오기({
    종류: 'watchtower', 날짜: '2026-08-24', 저장소, 만들기: 세는만들기({ 어느것: '파수대' }).만들기, 지금,
  });
  await 주간자료가져오기({
    종류: 'life-ministry', 날짜: '2026-08-24', 저장소, 만들기: 세는만들기({ 어느것: '생활과 봉사' }).만들기, 지금,
  });
  assert.deepEqual(저장소.담긴것.get('weeks/watchtower/2026-08-24.json').자료, { 어느것: '파수대' });
  assert.deepEqual(저장소.담긴것.get('weeks/life-ministry/2026-08-24.json').자료, { 어느것: '생활과 봉사' });
});

test('저장이 실패해도 만든 것을 돌려준다', async () => {
  const 저장소 = 가짜저장소();
  저장소.쓰기 = async () => {
    throw new Error('저장소가 없다');
  };
  const { 만들기 } = 세는만들기();
  const 결과 = await 주간자료가져오기({
    종류: 'watchtower', 날짜: '2026-08-27', 저장소, 만들기, 지금,
  });
  // OpenAI 호출이 이미 끝나 돈이 나갔다. 저장 실패로 그것을 잃게 하지 않는다.
  assert.deepEqual(결과.자료, { title: '새로 만든 것' });
  assert.equal(결과.새로만듦, true);
});

test('읽기가 실패하면 없는 것으로 보고 만든다', async () => {
  const 저장소 = 가짜저장소();
  저장소.읽기 = async () => {
    throw new Error('저장소가 없다');
  };
  const { 만들기, 부른것 } = 세는만들기();
  const 결과 = await 주간자료가져오기({
    종류: 'watchtower', 날짜: '2026-08-27', 저장소, 만들기, 지금,
  });
  assert.equal(결과.새로만듦, true);
  assert.deepEqual(부른것, ['2026-08-24']);
});

test('만들기가 실패하면 그대로 던진다', async () => {
  const 저장소 = 가짜저장소();
  const 만들기 = async () => {
    throw new Error('WOL 을 읽지 못했다');
  };
  // 자료를 못 만든 것은 삼키면 안 된다. 화면이 무엇이 잘못됐는지 알려야 한다.
  await assert.rejects(
    () => 주간자료가져오기({ 종류: 'watchtower', 날짜: '2026-08-27', 저장소, 만들기, 지금 }),
    /WOL/);
});

test('날짜가 모양에 안 맞으면 만들기를 부르지 않고 던진다', async () => {
  const { 만들기, 부른것 } = 세는만들기();
  await assert.rejects(
    () => 주간자료가져오기({ 종류: 'watchtower', 날짜: '오늘', 저장소: 가짜저장소(), 만들기, 지금 }),
    /날짜/);
  assert.deepEqual(부른것, []);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --test --no-warnings src/week-cache.test.mjs`
Expected: FAIL — `Cannot find module './week-cache.mjs'`

- [ ] **Step 3: 최소 구현을 쓴다**

`src/week-cache.mjs`

```js
// 주간 자료는 사람과 무관하므로 한 번 만들어 회중이 나눠 쓴다. 그 판단을 여기 모은다.
import { 주간열쇠, 주간경로 } from './storage-paths.mjs';

export async function 주간자료가져오기({
  종류,
  날짜,
  저장소,
  만들기,
  다시만들기 = false,
  지금 = () => Date.now(),
}) {
  // 경로 만들기는 try 밖에 둔다. 여기서 던지는 것은 잘못된 입력이지 저장소 문제가 아니다.
  const 경로 = 주간경로(종류, 날짜);
  const 주간 = 주간열쇠(날짜);

  if (!다시만들기) {
    // 읽기 는 없거나 판이 다르거나 실패하면 null 을 준다. store.mjs 가 이미 삼킨다.
    const 담긴것 = await 저장소.읽기(경로).catch(() => null);
    if (담긴것?.자료) {
      return { 자료: 담긴것.자료, 만든때: 담긴것.만든때 ?? null, 새로만듦: false };
    }
  }

  // 만들기 가 던지는 것은 삼키지 않는다. 자료를 못 만든 것은 화면이 알아야 한다.
  const 자료 = await 만들기(주간);
  const 만든때 = new Date(지금()).toISOString();

  try {
    await 저장소.쓰기(경로, { 자료, 만든때 });
  } catch {
    // 이 시점에 OpenAI 호출은 끝났고 돈은 이미 나갔다.
    // 저장 실패로 결과를 잃게 하는 쪽이 훨씬 나쁘다. 다음에 열면 없을 뿐이다.
  }

  return { 자료, 만든때, 새로만듦: true };
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --test --no-warnings src/week-cache.test.mjs`
Expected: PASS, 9 tests

- [ ] **Step 5: 커밋한다**

```bash
git add src/week-cache.mjs src/week-cache.test.mjs
git commit -m "주간 자료를 한 번만 만들어 나눠 쓰는 캐시 판단을 만든다"
```

---

### Task 3: 주간 캐시를 두 surface 에 붙인다

**Files:**
- Modify: `api/watchtower.js`, `api/life-ministry.js`, `src/web-server.mjs`

**Interfaces:**
- Consumes: `주간자료가져오기` (Task 2), `저장소만들기` (`src/store.mjs`), `가드` (`src/api-guard.mjs`)
- Produces: `GET`·`POST /api/watchtower`, `GET`·`POST /api/life-ministry` 가
  `{...자료, 보관: {만든때, 새로만듦}}` 을 돌려준다

`보관` 을 자료 옆에 얹는 이유가 있다. 지금 화면은 응답 전체를 `준비결과` 로 쓴다.
자료를 한 겹 더 감싸면 화면의 모든 자리를 고쳐야 한다. 필드 하나를 더하는 편이
변경 범위가 훨씬 작다.

- [ ] **Step 1: Vercel 어댑터 둘을 고친다**

`api/watchtower.js` 를 통째로 바꾼다.

```js
// Vercel에서 파수대 연구 준비 자료를 제공하는 Serverless Function
import * as blob from '@vercel/blob';
import { prepareWatchtower } from '../src/prep-service.mjs';
import { 저장소만들기 } from '../src/store.mjs';
import { 주간자료가져오기 } from '../src/week-cache.mjs';
import { 가드 } from '../src/api-guard.mjs';

async function handler(req, res) {
  try {
    const 날짜 = new URL(req.url, 'http://localhost').searchParams.get('date');
    const { 자료, 만든때, 새로만듦 } = await 주간자료가져오기({
      종류: 'watchtower',
      날짜,
      저장소: 저장소만들기(blob),
      만들기: 주간 => prepareWatchtower(주간, process.cwd()),
      // 돈 드는 동작을 GET 에 두지 않는다. 새로고침이나 미리 가져오기로 나가면 안 된다.
      다시만들기: req.method === 'POST',
    });
    res.status(200).json({ ...자료, 보관: { 만든때, 새로만듦 } });
  } catch (e) {
    res.status(500).json({ error: e.message });
  }
}

export default 가드(handler);
```

`api/life-ministry.js` 도 같은 모양으로 바꾼다. `prepareWatchtower` 대신
`prepareLifeAndMinistry` 를, 종류는 `'life-ministry'` 를 쓴다.

```js
// Vercel에서 생활과 봉사 준비 자료를 제공하는 Serverless Function
import * as blob from '@vercel/blob';
import { prepareLifeAndMinistry } from '../src/prep-service.mjs';
import { 저장소만들기 } from '../src/store.mjs';
import { 주간자료가져오기 } from '../src/week-cache.mjs';
import { 가드 } from '../src/api-guard.mjs';

async function handler(req, res) {
  try {
    const 날짜 = new URL(req.url, 'http://localhost').searchParams.get('date');
    const { 자료, 만든때, 새로만듦 } = await 주간자료가져오기({
      종류: 'life-ministry',
      날짜,
      저장소: 저장소만들기(blob),
      만들기: 주간 => prepareLifeAndMinistry(주간, process.cwd()),
      다시만들기: req.method === 'POST',
    });
    res.status(200).json({ ...자료, 보관: { 만든때, 새로만듦 } });
  } catch (e) {
    res.status(500).json({ error: e.message });
  }
}

export default 가드(handler);
```

- [ ] **Step 2: 로컬 서버를 같이 고친다**

`src/web-server.mjs` 의 맨 위 import 에 셋을 더한다.

```js
import * as blob from '@vercel/blob';
import { 저장소만들기 } from './store.mjs';
import { 주간자료가져오기 } from './week-cache.mjs';
```

모듈 스코프에 저장소를 한 번만 만들어 둔다. `환경가져오기` 옆에 놓는다.

```js
// 저장소는 상태가 없다. 요청마다 새로 만들 이유가 없다.
let 캐시된저장소 = null;
function 저장소가져오기() {
  return 캐시된저장소 ??= 저장소만들기(blob);
}
```

기존 `/api/watchtower` 와 `/api/life-ministry` 분기 둘을 이렇게 바꾼다.

```js
    if (url.pathname === '/api/watchtower' || url.pathname === '/api/life-ministry') {
      const 종류 = url.pathname === '/api/watchtower' ? 'watchtower' : 'life-ministry';
      const 만들기 = 종류 === 'watchtower'
        ? 주간 => prepareWatchtower(주간, root)
        : 주간 => prepareLifeAndMinistry(주간, root);
      const { 자료, 만든때, 새로만듦 } = await 주간자료가져오기({
        종류,
        날짜: url.searchParams.get('date'),
        저장소: 저장소가져오기(),
        만들기,
        다시만들기: req.method === 'POST',
      });
      json(res, 200, { ...자료, 보관: { 만든때, 새로만듦 } });
      return;
    }
```

- [ ] **Step 3: 전체 시험을 돌린다**

Run: `npm test`
Expected: 전부 통과.

- [ ] **Step 4: 손으로 확인한다**

```bash
rm -rf web/dist && npm run build:web
node --env-file-if-exists=.env --no-warnings src/web-server.mjs
```

다른 창에서 두 번 부른다.

```bash
curl -s "http://localhost:3000/api/watchtower?date=2026-08-27" -b "jw_session=<브라우저에서 복사>" | head -c 200
```

세션 쿠키가 필요하다. 브라우저로 로그인한 뒤 개발자 도구에서 `jw_session` 값을
복사해 쓴다. **그 값을 보고서에 적지 않는다.**

Expected: 처음에는 `"새로만듦":true`, 두 번째에는 `"새로만듦":false` 이고 두 번째가
훨씬 빠르다. 두 번째가 `true` 면 저장이 안 된 것이니 Blob 연결을 확인한다.

브라우저로 확인하기 어려우면 이 단계는 건너뛰고 보고서에 그렇게 적는다.
**되지 않은 것을 되었다고 적지 않는다.**

- [ ] **Step 5: 커밋한다**

```bash
git add api/watchtower.js api/life-ministry.js src/web-server.mjs
git commit -m "주간 답변을 한 번 만들어 회중이 나눠 쓰게 한다"
```

---

### Task 4: 설정을 서버로 옮긴다

**Files:**
- Create: `api/my-profile.js`
- Modify: `src/auth-service.mjs`, `api/options.js`, `src/web-server.mjs`
- Test: `src/auth-service.test.mjs`

**Interfaces:**
- Consumes: `설정경로` (Task 1), `기본프로필` (`src/talk-profile.mjs`)
- Produces:
  - `인증만들기(...)` 결과에 `설정읽기(회원번호)` 와 `설정쓰기(회원번호, 설정)` 이 는다
  - `GET /api/options` 응답에 `설정` 이 실린다
  - `POST /api/my-profile` 이 `{설정}` 을 받아 저장한다

- [ ] **Step 1: 실패하는 시험을 쓴다**

`src/auth-service.test.mjs` 끝에 더한다. 기존 시험은 건드리지 않는다.

```js
test('설정이 없으면 기본값을 준다', async () => {
  const 인증 = 만들기(가짜저장소());
  const 설정 = await 인증.설정읽기('777');
  assert.equal(설정.성별, '형제');
  assert.equal(설정.연령, 35);
  assert.equal(설정.임명, '미임명');
});

test('저장된 설정을 기본값 위에 얹는다', async () => {
  const 저장소 = 가짜저장소({
    'users/777/profile.json': { 회원번호: '777', 닉네임: '동언', 설정: { 임명: '장로' } },
  });
  const 설정 = await 만들기(저장소).설정읽기('777');
  assert.equal(설정.임명, '장로');
  // 저장할 때 없던 항목은 기본값으로 채워야 화면이 빈칸을 그리지 않는다.
  assert.equal(설정.성별, '형제');
  assert.equal(설정.분당글자수, 320);
});

test('설정을 저장해도 닉네임이 지워지지 않는다', async () => {
  const 저장소 = 가짜저장소({
    'users/777/profile.json': { 회원번호: '777', 닉네임: '동언', 승인된때: '2026-08-27T00:00:00.000Z' },
  });
  await 만들기(저장소).설정쓰기('777', { 임명: '장로', 연령: 40 });
  const 기록 = 저장소.담긴것.get('users/777/profile.json');
  assert.equal(기록.닉네임, '동언');
  assert.equal(기록.승인된때, '2026-08-27T00:00:00.000Z');
  assert.equal(기록.설정.임명, '장로');
  assert.equal(기록.설정.연령, 40);
});

test('설정에 모르는 항목을 넣으면 버린다', async () => {
  const 저장소 = 가짜저장소();
  await 만들기(저장소).설정쓰기('777', { 임명: '장로', 회원번호: '999', 아무거나: 1 });
  const 담긴설정 = 저장소.담긴것.get('users/777/profile.json').설정;
  assert.equal(담긴설정.임명, '장로');
  assert.equal('회원번호' in 담긴설정, false);
  assert.equal('아무거나' in 담긴설정, false);
});

test('설정 저장이 실패하면 던진다', async () => {
  const 저장소 = 가짜저장소();
  저장소.쓰기 = async () => {
    throw new Error('저장소가 없다');
  };
  // 형제가 직접 누른 동작이다. 조용히 넘기면 저장된 줄 알고 나간다.
  await assert.rejects(() => 만들기(저장소).설정쓰기('777', { 임명: '장로' }));
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --test --no-warnings src/auth-service.test.mjs`
Expected: FAIL — `인증.설정읽기 is not a function`

- [ ] **Step 3: 서비스에 설정 읽기·쓰기를 더한다**

`src/auth-service.mjs` 위쪽 import 에 더한다.

```js
import { 기본프로필 } from './talk-profile.mjs';
```

`인증만들기` 가 돌려주는 객체에 둘을 더한다. `초대` 관련 코드는 이미 없다.

```js
    async 설정읽기(회원번호) {
      const 기록 = await 저장소.읽기(설정경로(회원번호));
      // 저장된 뒤에 항목이 늘 수 있다. 기본값 위에 얹어야 화면이 빈칸을 그리지 않는다.
      return { ...기본프로필(), ...(기록?.설정 ?? {}) };
    },

    async 설정쓰기(회원번호, 설정) {
      const 경로 = 설정경로(회원번호);
      const 기록 = (await 저장소.읽기(경로)) ?? {};
      // 브라우저가 보낸 것을 그대로 담지 않는다. 기본프로필 에 있는 열쇠만 통과시킨다.
      const 걸러낸것 = {};
      for (const 열쇠 of Object.keys(기본프로필())) {
        if (열쇠 in 설정) 걸러낸것[열쇠] = 설정[열쇠];
      }
      // 던지는 것을 삼키지 않는다. 형제가 직접 누른 동작이라 실패를 알아야 한다.
      await 저장소.쓰기(경로, { ...기록, 설정: { ...기본프로필(), ...걸러낸것 } });
    },
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --test --no-warnings src/auth-service.test.mjs`
Expected: PASS. 새 시험 5개를 포함해 모두 통과한다.

- [ ] **Step 5: `options` 응답에 설정을 싣는다**

`api/options.js` 를 통째로 바꾼다.

```js
// Vercel에서 주간 선택 옵션과 내 설정을 제공하는 Serverless Function
import { buildWeekOptions, localToday } from '../src/web-options.mjs';
import { 인증가져오기 } from '../src/auth-runtime.mjs';
import { 가드 } from '../src/api-guard.mjs';

async function handler(req, res) {
  res.status(200).json({
    today: localToday().toISOString().slice(0, 10),
    weeks: buildWeekOptions(),
    사용자: { 닉네임: req.사용자.닉네임 },
    설정: await 인증가져오기().설정읽기(req.사용자.회원번호),
  });
}

export default 가드(handler);
```

- [ ] **Step 6: 설정 저장 어댑터를 만든다**

`api/my-profile.js`

```js
// Vercel에서 내 설정을 저장하는 어댑터
import { 인증가져오기 } from '../src/auth-runtime.mjs';
import { 가드 } from '../src/api-guard.mjs';

async function handler(req, res) {
  if (req.method !== 'POST') {
    res.setHeader('Allow', 'POST');
    res.status(405).json({ error: '허용되지 않는 방식입니다.' });
    return;
  }
  try {
    await 인증가져오기().설정쓰기(req.사용자.회원번호, req.body?.설정 ?? {});
    res.status(200).json({ 저장됨: true });
  } catch {
    res.status(500).json({ error: '설정을 저장하지 못했습니다. 잠시 뒤 다시 시도해 주십시오.' });
  }
}

export default 가드(handler);
```

- [ ] **Step 7: 로컬 서버에 같은 둘을 붙인다**

`src/web-server.mjs` 의 `/api/options` 분기를 이렇게 바꾼다.

```js
    if (url.pathname === '/api/options') {
      json(res, 200, {
        today: localToday().toISOString().slice(0, 10),
        weeks: buildWeekOptions(),
        사용자: { 닉네임: req.사용자.닉네임 },
        설정: await 인증가져오기().설정읽기(req.사용자.회원번호),
      });
      return;
    }
```

`인증가져오기` 는 이미 이 파일에 import 되어 있다. 없으면 더한다.

그 아래에 새 분기를 더한다.

```js
    if (url.pathname === '/api/my-profile' && req.method === 'POST') {
      const 몸 = await 본문읽기(req);
      try {
        await 인증가져오기().설정쓰기(req.사용자.회원번호, 몸?.설정 ?? {});
        json(res, 200, { 저장됨: true });
      } catch {
        json(res, 500, { error: '설정을 저장하지 못했습니다. 잠시 뒤 다시 시도해 주십시오.' });
      }
      return;
    }
```

- [ ] **Step 8: 전체 시험을 돌린다**

Run: `npm test`
Expected: 전부 통과.

- [ ] **Step 9: 커밋한다**

```bash
git add src/auth-service.mjs src/auth-service.test.mjs api/options.js api/my-profile.js src/web-server.mjs
git commit -m "화자 설정을 서버에 두어 기기가 바뀌어도 따라오게 한다"
```

---

### Task 5: 연설 자료 저장과 복원

**Files:**
- Create: `src/talk-store.mjs`
- Test: `src/talk-store.test.mjs`

**Interfaces:**
- Consumes: `연설경로` (Task 1)
- Produces:
  - `연설읽기({저장소, 회원번호, 주간, 배정번호, 배정제목}) -> object | null`
  - `연설쓰기({저장소, 회원번호, 주간, 배정번호, 배정제목, 내용, 지금?}) -> void`

- [ ] **Step 1: 실패하는 시험을 쓴다**

`src/talk-store.test.mjs`

```js
// 연설 자료의 저장과 복원을 검증하는 테스트
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { 연설읽기, 연설쓰기 } from './talk-store.mjs';

function 가짜저장소(초기 = {}) {
  const 담긴것 = new Map(Object.entries(초기));
  return {
    담긴것,
    async 읽기(경로) { return 담긴것.get(경로) ?? null; },
    async 쓰기(경로, 내용) { 담긴것.set(경로, 내용); },
    async 목록() { return [...담긴것.keys()]; },
    async 지우기(경로) { 담긴것.delete(경로); },
  };
}

const 지금 = () => 1_800_000_000_000;
const 기본 = { 회원번호: '777', 주간: '2026-08-27', 배정번호: 2, 배정제목: '보물 — 징계' };

test('쓴 것을 그대로 읽는다', async () => {
  const 저장소 = 가짜저장소();
  await 연설쓰기({ ...기본, 저장소, 내용: { 뼈대: { 배정시간: 600 } }, 지금 });
  const 읽은것 = await 연설읽기({ ...기본, 저장소 });
  assert.deepEqual(읽은것.뼈대, { 배정시간: 600 });
  assert.equal(읽은것.만든때, new Date(지금()).toISOString());
});

test('정규화된 주간으로 저장한다', async () => {
  const 저장소 = 가짜저장소();
  await 연설쓰기({ ...기본, 저장소, 내용: { 뼈대: {} }, 지금 });
  assert.equal(저장소.담긴것.has('users/777/talks/2026-08-24-2.json'), true);
});

test('같은 주의 다른 날짜로도 찾는다', async () => {
  const 저장소 = 가짜저장소();
  await 연설쓰기({ ...기본, 주간: '2026-08-24', 저장소, 내용: { 뼈대: { 배정시간: 600 } }, 지금 });
  const 읽은것 = await 연설읽기({ ...기본, 주간: '2026-08-30', 저장소 });
  assert.deepEqual(읽은것.뼈대, { 배정시간: 600 });
});

test('없으면 null 이다', async () => {
  assert.equal(await 연설읽기({ ...기본, 저장소: 가짜저장소() }), null);
});

test('배정 제목이 다르면 없는 것으로 본다', async () => {
  const 저장소 = 가짜저장소();
  await 연설쓰기({ ...기본, 저장소, 내용: { 뼈대: {} }, 지금 });
  // 교재 페이지가 고쳐져 순서가 바뀌면 같은 번호가 다른 배정을 가리킨다.
  // 틀린 원고를 맞는 것처럼 보여 주는 쪽이 다시 만드는 것보다 나쁘다.
  const 읽은것 = await 연설읽기({ ...기본, 배정제목: '학생연설 — 다른 것', 저장소 });
  assert.equal(읽은것, null);
});

test('다시 쓰면 덮어쓴다', async () => {
  const 저장소 = 가짜저장소();
  await 연설쓰기({ ...기본, 저장소, 내용: { 뼈대: { 배정시간: 600 } }, 지금 });
  await 연설쓰기({ ...기본, 저장소, 내용: { 뼈대: { 배정시간: 300 } }, 지금 });
  const 읽은것 = await 연설읽기({ ...기본, 저장소 });
  assert.deepEqual(읽은것.뼈대, { 배정시간: 300 });
  assert.equal(저장소.담긴것.size, 1);
});

test('저장이 실패해도 던지지 않는다', async () => {
  const 저장소 = 가짜저장소();
  저장소.쓰기 = async () => {
    throw new Error('저장소가 없다');
  };
  // 원고는 이미 만들어져 화면으로 갈 참이다. 저장 실패로 그것을 잃게 하지 않는다.
  await 연설쓰기({ ...기본, 저장소, 내용: { 뼈대: {} }, 지금 });
});

test('경로를 만들 수 없는 입력은 저장 실패로 삼키지 않고 던진다', async () => {
  const 저장소 = 가짜저장소();
  // 잘못된 입력을 저장소 문제로 덮어 버리면 원인을 못 찾는다.
  await assert.rejects(
    () => 연설쓰기({ ...기본, 배정번호: 1.5, 저장소, 내용: {}, 지금 }),
    /배정번호/);
  await assert.rejects(
    () => 연설쓰기({ ...기본, 회원번호: '../etc', 저장소, 내용: {}, 지금 }),
    /회원번호/);
});

test('읽기가 실패하면 null 이다', async () => {
  const 저장소 = 가짜저장소();
  저장소.읽기 = async () => {
    throw new Error('저장소가 없다');
  };
  assert.equal(await 연설읽기({ ...기본, 저장소 }), null);
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `node --test --no-warnings src/talk-store.test.mjs`
Expected: FAIL — `Cannot find module './talk-store.mjs'`

- [ ] **Step 3: 최소 구현을 쓴다**

`src/talk-store.mjs`

```js
// 연설 자료를 사람별로 보관한다. 배정 번호가 가리키는 대상이 바뀌었으면 복원하지 않는다.
import { 연설경로 } from './storage-paths.mjs';

export async function 연설읽기({ 저장소, 회원번호, 주간, 배정번호, 배정제목 }) {
  const 경로 = 연설경로(회원번호, 주간, 배정번호);
  const 담긴것 = await 저장소.읽기(경로).catch(() => null);
  if (!담긴것) return null;
  // 교재 페이지가 고쳐져 배정 순서가 바뀌면 같은 번호가 다른 배정을 가리킨다.
  // 틀린 원고를 맞는 것처럼 보여 주는 쪽이 다시 만드는 것보다 나쁘다.
  if (담긴것.배정제목 !== 배정제목) return null;
  return 담긴것;
}

export async function 연설쓰기({
  저장소, 회원번호, 주간, 배정번호, 배정제목, 내용, 지금 = () => Date.now(),
}) {
  // 경로 만들기는 try 밖에 둔다. 잘못된 입력을 저장소 문제로 덮으면 원인을 못 찾는다.
  const 경로 = 연설경로(회원번호, 주간, 배정번호);
  try {
    await 저장소.쓰기(경로, {
      배정제목,
      만든때: new Date(지금()).toISOString(),
      ...내용,
    });
  } catch {
    // 원고는 이미 만들어져 화면으로 간다. 저장 실패로 그것을 잃게 하지 않는다.
  }
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `node --test --no-warnings src/talk-store.test.mjs`
Expected: PASS, 9 tests

- [ ] **Step 5: 커밋한다**

```bash
git add src/talk-store.mjs src/talk-store.test.mjs
git commit -m "연설 자료를 사람별로 보관하는 모듈을 만든다"
```

---

### Task 6: 연설 저장·복원을 두 surface 에 붙인다

**Files:**
- Create: `api/my-talk.js`
- Modify: `api/talk-outline.js`, `api/talk-draft.js`, `src/web-server.mjs`

**Interfaces:**
- Consumes: `연설읽기`·`연설쓰기` (Task 5), `저장소만들기`, `가드`
- Produces:
  - `POST /api/talk-outline` 이 몸에 `주간`·`배정번호`·`배정제목` 을 더 받는다
  - `POST /api/talk-draft` 도 같다
  - `GET /api/my-talk?주간=&배정번호=&제목=` 이 저장된 자료 또는 `null` 을 준다

`배정제목` 을 질의 문자열로 받는 이유가 있다. 서버가 대조해야 시험이 덮는다.
화면에서 대조하면 `node --test` 가 닿지 않는다.

- [ ] **Step 1: 뼈대 어댑터를 고친다**

`api/talk-outline.js` 를 통째로 바꾼다.

```js
// Vercel 서버리스에서 연설 뼈대를 만드는 어댑터
import * as blob from '@vercel/blob';
import { 환경만들기, 뼈대준비 } from '../src/talk-service.mjs';
import { 저장소만들기 } from '../src/store.mjs';
import { 연설쓰기 } from '../src/talk-store.mjs';
import { 가드 } from '../src/api-guard.mjs';

// 환경만들기 가 성경 인덱스를 새로 읽는 데 요청당 150~175ms 걸린다(src/web-server.mjs 실측 주석 참고).
// Vercel 서버리스도 함수 인스턴스가 재사용되는 동안은 모듈 스코프가 살아 있으므로 한 번만 만들어 재사용한다.
let 캐시된환경 = null;
function 환경가져오기() {
  return 캐시된환경 ??= 환경만들기(process.cwd());
}

async function handler(req, res) {
  try {
    const 몸 = req.body ?? {};
    const 결과 = await 뼈대준비(몸, 환경가져오기());
    await 연설쓰기({
      저장소: 저장소만들기(blob),
      회원번호: req.사용자.회원번호,
      주간: 몸.주간,
      배정번호: 몸.배정번호,
      배정제목: 몸.배정제목,
      내용: { 뼈대: 결과.뼈대 },
    });
    res.status(200).json(결과);
  } catch (e) {
    res.status(500).json({ error: e.message });
  }
}

export default 가드(handler);
```

- [ ] **Step 2: 원고 어댑터를 고친다**

`api/talk-draft.js` 를 통째로 바꾼다. 원고를 저장할 때 뼈대도 같이 넣어야
복원 한 번으로 두 단계가 다 살아난다.

```js
// Vercel 서버리스에서 연설 원고를 만드는 어댑터
import * as blob from '@vercel/blob';
import { 환경만들기, 원고준비 } from '../src/talk-service.mjs';
import { 저장소만들기 } from '../src/store.mjs';
import { 연설쓰기 } from '../src/talk-store.mjs';
import { 가드 } from '../src/api-guard.mjs';

// 환경만들기 가 성경 인덱스를 새로 읽는 데 요청당 150~175ms 걸린다(src/web-server.mjs 실측 주석 참고).
// Vercel 서버리스도 함수 인스턴스가 재사용되는 동안은 모듈 스코프가 살아 있으므로 한 번만 만들어 재사용한다.
let 캐시된환경 = null;
function 환경가져오기() {
  return 캐시된환경 ??= 환경만들기(process.cwd());
}

async function handler(req, res) {
  try {
    const 몸 = req.body ?? {};
    const 결과 = await 원고준비(몸, 환경가져오기());
    // 뼈대도 함께 넣는다. 복원 한 번으로 두 단계가 다 살아나야 한다.
    await 연설쓰기({
      저장소: 저장소만들기(blob),
      회원번호: req.사용자.회원번호,
      주간: 몸.주간,
      배정번호: 몸.배정번호,
      배정제목: 몸.배정제목,
      내용: { 뼈대: 몸.뼈대, 원고: 결과 },
    });
    res.status(200).json(결과);
  } catch (e) {
    res.status(500).json({ error: e.message });
  }
}

export default 가드(handler);
```

- [ ] **Step 3: 복원 어댑터를 만든다**

`api/my-talk.js`

```js
// Vercel에서 저장된 연설 자료를 하나 돌려주는 어댑터
import * as blob from '@vercel/blob';
import { 저장소만들기 } from '../src/store.mjs';
import { 연설읽기 } from '../src/talk-store.mjs';
import { 가드 } from '../src/api-guard.mjs';

async function handler(req, res) {
  try {
    const 질의 = new URL(req.url, 'http://localhost').searchParams;
    const 자료 = await 연설읽기({
      저장소: 저장소만들기(blob),
      회원번호: req.사용자.회원번호,
      주간: 질의.get('주간'),
      배정번호: Number(질의.get('배정번호')),
      배정제목: 질의.get('제목'),
    });
    res.status(200).json({ 자료 });
  } catch {
    // 모양이 틀린 요청이다. 저장된 게 없는 것과 같이 다뤄 화면을 막지 않는다.
    res.status(200).json({ 자료: null });
  }
}

export default 가드(handler);
```

- [ ] **Step 4: 로컬 서버를 같이 고친다**

`src/web-server.mjs` 의 import 에 하나를 더한다.

```js
import { 연설읽기, 연설쓰기 } from './talk-store.mjs';
```

`/api/talk-outline` 과 `/api/talk-draft` 분기를 이렇게 바꾼다.

```js
    if (url.pathname === '/api/talk-outline' && req.method === 'POST') {
      const 몸 = await 본문읽기(req);
      const 결과 = await 뼈대준비(몸, 환경가져오기());
      await 연설쓰기({
        저장소: 저장소가져오기(),
        회원번호: req.사용자.회원번호,
        주간: 몸.주간,
        배정번호: 몸.배정번호,
        배정제목: 몸.배정제목,
        내용: { 뼈대: 결과.뼈대 },
      });
      json(res, 200, 결과);
      return;
    }
    if (url.pathname === '/api/talk-draft' && req.method === 'POST') {
      const 몸 = await 본문읽기(req);
      const 결과 = await 원고준비(몸, 환경가져오기());
      await 연설쓰기({
        저장소: 저장소가져오기(),
        회원번호: req.사용자.회원번호,
        주간: 몸.주간,
        배정번호: 몸.배정번호,
        배정제목: 몸.배정제목,
        내용: { 뼈대: 몸.뼈대, 원고: 결과 },
      });
      json(res, 200, 결과);
      return;
    }
```

그 아래에 복원 분기를 더한다.

```js
    if (url.pathname === '/api/my-talk') {
      try {
        json(res, 200, {
          자료: await 연설읽기({
            저장소: 저장소가져오기(),
            회원번호: req.사용자.회원번호,
            주간: url.searchParams.get('주간'),
            배정번호: Number(url.searchParams.get('배정번호')),
            배정제목: url.searchParams.get('제목'),
          }),
        });
      } catch {
        json(res, 200, { 자료: null });
      }
      return;
    }
```

- [ ] **Step 5: 전체 시험을 돌린다**

Run: `npm test`
Expected: 전부 통과.

- [ ] **Step 6: 커밋한다**

```bash
git add api/talk-outline.js api/talk-draft.js api/my-talk.js src/web-server.mjs
git commit -m "연설 뼈대와 원고를 만들 때마다 서버에 남긴다"
```

---

### Task 7: 화면 — 저장 안내 띠와 다시 만들기

**Files:**
- Create: `web/src/components/ui/alert-dialog.tsx` (shadcn 에서 받는다)
- Create: `web/src/components/SavedNotice.tsx`
- Modify: `web/src/lib/api.ts`, `web/src/routes/StudyPrep.tsx`, `web/src/smoke.tsx`

**Interfaces:**
- Consumes: `GET`·`POST /api/watchtower`·`/api/life-ministry` 응답의 `보관` (Task 3)
- Produces: `준비결과` 타입에 `보관?: {만든때: string|null; 새로만듦: boolean}` 이 는다

- [ ] **Step 1: shadcn 에서 AlertDialog 를 받는다**

```bash
npx shadcn@latest add alert-dialog
```

**받은 뒤 반드시 확인한다.** 이 저장소에서 shadcn CLI 가 엉뚱한 곳에 쓴 적이 있다.

```bash
git status --porcelain
```

Expected: `web/src/components/ui/alert-dialog.tsx` 하나만 새로 생겨야 한다.

- `src/components/` 아래에 무언가 생겼으면 **지운다.** 거기는 서버 코드 자리다
- `web/src/index.css` 가 바뀌었으면 **되돌린다.** 색과 글꼴이 이미 맞춰져 있다
- `package.json` 에 의존성이 늘었으면 보고한다. 이 계획은 새 의존성을 넣지 않는다

- [ ] **Step 2: `api.ts` 에 타입과 호출을 더한다**

`준비결과` 타입에 한 줄을 더한다.

```ts
  보관?: { 만든때: string | null; 새로만듦: boolean };
```

파일 끝에 다시 만들기 호출을 더한다.

```ts
export function 다시만들기(종류: 준비종류, 날짜: string) {
  // 돈 드는 동작이라 POST 다. GET 에 두면 새로고침만으로도 호출이 나간다.
  return 요청<준비결과>(`/api/${종류}?date=${encodeURIComponent(날짜)}`, { method: 'POST' });
}
```

- [ ] **Step 3: 안내 띠 부품을 만든다**

`web/src/components/SavedNotice.tsx`

```tsx
// 저장된 자료임을 알리고 다시 만들 수 있게 한다. 다시 만들면 회중 모두의 것이 바뀐다.
import { useState } from 'react';
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
} from '@/components/ui/alert-dialog';
import { Button } from '@/components/ui/button';
import { 판넬 } from '@/components/StatusBox';
import { cn } from '@/lib/utils';

export default function SavedNotice({
  만든때,
  다시만드는중,
  다시만들기,
}: {
  만든때: string | null;
  다시만드는중: boolean;
  다시만들기: () => void;
}) {
  const [열림, set열림] = useState(false);
  const 날짜 = 만든때 ? 만든때.slice(0, 10) : null;

  return (
    <div
      className={cn(
        판넬,
        'flex flex-col items-stretch gap-3 px-5 py-4 text-ink-muted sm:flex-row sm:items-center sm:justify-between',
      )}
      role="status"
    >
      <span>{날짜 ? `${날짜} 에 만들어진 답변입니다.` : '이미 만들어져 있던 답변입니다.'}</span>
      <Button
        type="button"
        variant="outline"
        onClick={() => set열림(true)}
        disabled={다시만드는중}
        aria-busy={다시만드는중}
        className="min-h-11 shrink-0 font-bold"
      >
        다시 만들기
      </Button>

      <AlertDialog open={열림} onOpenChange={set열림}>
        <AlertDialogContent>
          <AlertDialogHeader>
            <AlertDialogTitle>다시 만들까요?</AlertDialogTitle>
            <AlertDialogDescription>
              이번 주 답변이 이미 있습니다. 다시 만들면 이전 답변은 사라지고, 회중의 다른 분들께도
              새 답변이 보이게 됩니다.
            </AlertDialogDescription>
          </AlertDialogHeader>
          <AlertDialogFooter>
            <AlertDialogCancel className="min-h-11">그만두기</AlertDialogCancel>
            <AlertDialogAction
              className="min-h-11 font-bold"
              onClick={() => {
                set열림(false);
                다시만들기();
              }}
            >
              다시 만들기
            </AlertDialogAction>
          </AlertDialogFooter>
        </AlertDialogContent>
      </AlertDialog>
    </div>
  );
}
```

**받은 파일이 실제로 무엇을 내보내는지 먼저 확인한다.** 이 저장소의 shadcn 은
Radix 가 아니라 Base UI 라 이름이 다를 수 있다.

```bash
grep -n "^export\|^function\|^const" web/src/components/ui/alert-dialog.tsx
```

위 코드의 import 목록과 다르면 **받은 파일이 내보내는 이름에 맞춘다.** 프롭 이름도
같은 방식으로 확인한다. Base UI 는 `open`/`onOpenChange` 대신 다른 이름을 쓸 수
있다. **`window.confirm` 으로 바꾸지 않는다.** 브라우저 기본
대화상자는 화면 자동 검증 도구를 멈춰 세운다.

- [ ] **Step 4: `StudyPrep` 에 붙인다**

`web/src/routes/StudyPrep.tsx` 를 고친다. import 에 둘을 더한다.

```tsx
import SavedNotice from '@/components/SavedNotice';
import { 준비자료, 다시만들기 as 다시만들기호출, type 준비결과, type 준비종류 } from '@/lib/api';
```

상태 하나를 더한다.

```tsx
  const [다시만드는중, set다시만드는중] = useState(false);
```

`준비하기` 아래에 다시 만들기 함수를 더한다.

```tsx
  async function 다시만들어보기() {
    set다시만드는중(true);
    set오류('');
    try {
      set자료(await 다시만들기호출(종류, 고른주));
    } catch (실패) {
      set오류(실패 instanceof Error ? 실패.message : '요청에 실패했습니다.');
    } finally {
      set다시만드는중(false);
    }
  }
```

종류가 바뀔 때 비우는 `useEffect` 에 한 줄을 더한다.

```tsx
    set다시만드는중(false);
```

결과 자리에 안내 띠를 넣는다. `{상태 === '완료' && 자료 && <StudyResult 자료={자료} />}`
줄을 이렇게 바꾼다.

```tsx
        {상태 === '완료' && 자료 && (
          <>
            {자료.보관 && !자료.보관.새로만듦 && (
              <SavedNotice
                만든때={자료.보관.만든때}
                다시만드는중={다시만드는중}
                다시만들기={다시만들어보기}
              />
            )}
            <StudyResult 자료={자료} />
          </>
        )}
```

**새로 만든 직후에는 띠를 보이지 않는다.** 방금 만든 것에 "저장된 답변입니다" 를
붙이면 형제가 헷갈리신다.

- [ ] **Step 5: 스모크에 새 부품이 걸리는지 본다**

`web/src/smoke.tsx` 는 그대로 두어도 `StudyPrep` 을 마운트하므로 새 import 가
깨지면 잡힌다. 고칠 것이 없다.

- [ ] **Step 6: 확인한다**

```bash
npx tsc --noEmit
npm run smoke:web
npm test
rm -rf web/dist && npm run build:web
```

Expected: 전부 통과. 스모크는 8화면 전부 렌더 성공이다.

`npm run build:web` 이 `STATUS_STACK_BUFFER_OVERRUN` 으로 죽으면 `web/dist` 를
지우고 다시 돌린다. Node 24 의 재귀 삭제가 이 저장소의 한글 경로에서 죽는 기존
문제이고 스크립트가 이미 앞에서 지우게 되어 있다.

- [ ] **Step 7: 커밋한다**

```bash
git add web/src/components/ui/alert-dialog.tsx web/src/components/SavedNotice.tsx web/src/lib/api.ts web/src/routes/StudyPrep.tsx
git commit -m "저장된 답변임을 알리고 다시 만들 수 있게 한다"
```

---

### Task 8: 화면 — 설정과 연설 복원 배선

**Files:**
- Modify: `web/src/lib/use-weeks.ts`, `web/src/lib/api.ts`, `web/src/lib/talk-api.ts`, `web/src/routes/Talk.tsx`
- Delete: `web/src/lib/talk-profile-store.ts`

**Interfaces:**
- Consumes: `GET /api/options` 의 `설정` (Task 4), `GET /api/my-talk` (Task 6)
- Produces: `use설정()` 훅 — `{설정, 불러옴, 설정저장}`

- [ ] **Step 1: `api.ts` 에 설정 타입과 저장 호출을 더한다**

`주간목록` 의 반환 타입에 `설정` 을 더한다.

```ts
export function 주간목록() {
  return 요청<{
    today: string;
    weeks: 주간[];
    사용자: { 닉네임: string };
    설정: 프로필;
  }>('/api/options');
}
```

파일 위쪽에 타입 import 를 더한다.

```ts
import type { 프로필 } from './talk-api';
```

파일 끝에 저장 호출을 더한다.

```ts
export function 설정저장하기(설정: 프로필) {
  return 요청<{ 저장됨: boolean }>('/api/my-profile', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ 설정 }),
  });
}
```

- [ ] **Step 2: `use설정` 훅을 만든다**

`web/src/lib/use-weeks.ts` 끝에 더한다. 같은 `한번만가져오기()` 를 쓰므로
요청은 여전히 하나만 나간다.

```ts
export function use설정() {
  const [설정, set설정] = useState<프로필 | null>(null);

  useEffect(() => {
    let 살아있음 = true;
    한번만가져오기().then(
      자료 => 살아있음 && set설정(자료.설정),
      () => {
        // 로그인이 필요하면 App 이 로그인 화면으로 보낸다. 여기서 따로 알리지 않는다.
      },
    );
    return () => {
      살아있음 = false;
    };
  }, []);

  async function 설정저장(다음: 프로필) {
    set설정(다음);
    await 설정저장하기(다음);
  }

  return { 설정, 불러옴: 설정 !== null, 설정저장 };
}
```

import 에 둘을 더한다.

```ts
import { 로그인필요오류, 설정저장하기, 주간목록, type 주간 } from './api';
import type { 프로필 } from './talk-api';
```

- [ ] **Step 3: `talk-api.ts` 에 복원 호출을 더하고 요청에 주간을 싣는다**

`뼈대만들기` 와 `원고만들기` 의 본문 타입에 셋을 더한다.

```ts
export function 뼈대만들기(본문: {
  배정: 배정;
  프로필: 프로필;
  공개강연입력?: 공개강연입력;
  주간: string;
  배정번호: number;
  배정제목: string;
}) {
  return 보내기<{ 뼈대: 뼈대; 생성?: { warning?: string } }>('/api/talk-outline', 본문);
}

export function 원고만들기(본문: {
  뼈대: 뼈대;
  프로필: 프로필;
  주간: string;
  배정번호: number;
  배정제목: string;
}) {
  return 보내기<{
    구조체: { 경고?: string[] };
    산출물: Record<산출물키, string>;
    시간: 시간정보;
    생성?: { warning?: string };
  }>('/api/talk-draft', 본문);
}
```

파일 끝에 복원 호출을 더한다.

```ts
export type 보관된연설 = {
  배정제목: string;
  만든때: string;
  뼈대?: 뼈대;
  원고?: {
    구조체: { 경고?: string[] };
    산출물: Record<산출물키, string>;
    시간: 시간정보;
    생성?: { warning?: string };
  };
};

export function 보관된연설가져오기(주간: string, 배정번호: number, 제목: string) {
  const 질의 = `주간=${encodeURIComponent(주간)}&배정번호=${배정번호}&제목=${encodeURIComponent(제목)}`;
  return 요청<{ 자료: 보관된연설 | null }>(`/api/my-talk?${질의}`);
}
```

- [ ] **Step 4: `Talk.tsx` 를 고친다**

import 에서 `talk-profile-store` 를 지우고 훅을 쓴다.

```tsx
import { use주간목록, use설정 } from '@/lib/use-weeks';
```

`const [프로필, set프로필] = useState(기본값);` 과 그 아래 `프로필읽기()` 를 쓰는
`useEffect` 를 지우고 이렇게 바꾼다.

```tsx
  const { 설정: 프로필, 불러옴: 설정불러옴, 설정저장 } = use설정();
```

`talk-api` import 블록에 프로필 타입을 별칭으로 더한다. 지역 변수 이름이
`프로필` 이라 같은 이름으로 들여오면 가려진다.

```tsx
  type 프로필 as 프로필형,
```

`프로필고침` 을 이렇게 바꾼다.

```tsx
  function 프로필고침(다음: 프로필형) {
    설정저장(다음).catch(실패 => {
      set설정오류(실패 instanceof Error ? 실패.message : '설정을 저장하지 못했습니다.');
    });
  }
```

상태 하나를 더한다.

```tsx
  const [설정오류, set설정오류] = useState('');
```

배정 목록을 부르는 `useEffect` 에서 `프로필읽기()` 를 지우고 `프로필` 을 쓴다.
설정을 아직 못 받았으면 부르지 않는다.

```tsx
  const 성별 = 프로필?.성별;
  const 임명 = 프로필?.임명;

  useEffect(() => {
    if (!고른주 || !프로필) return;
    let 살아있음 = true;
    set배정안내('배정을 불러오는 중입니다.');
    set배정오류('');
    set선택배정(null);
    산출물초기화();

    배정가져오기(고른주, 프로필).then(
      자료 => {
        if (!살아있음) return;
        set배정들([...자료.배정, 자료.공개강연카드].filter(Boolean) as 배정[]);
        set배정안내('');
      },
      실패 => {
        if (!살아있음) return;
        set배정들([]);
        set배정안내('');
        set배정오류(실패.message);
      },
    );
    return () => {
      살아있음 = false;
    };
    // 자격판정은 성별과 임명만 본다. 나머지 항목까지 걸면 글자 하나 칠 때마다 API 를 두드리게 된다.
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [고른주, 성별, 임명, 산출물초기화]);
```

`배정고르기` 에서 저장된 것을 복원한다. 배정 번호는 `배정들` 안의 순번이다.

```tsx
  function 배정고르기(값: 배정) {
    set선택배정(값);
    산출물초기화();
    const 번호 = 배정들.indexOf(값);
    if (번호 < 0 || !고른주) return;
    보관된연설가져오기(고른주, 번호, 값.제목).then(
      ({ 자료 }) => {
        if (!자료) return;
        if (자료.뼈대) set뼈대(자료.뼈대);
        // 서버는 API 응답을 그대로 저장한다. 화면 상태는 모양이 달라 여기서 맞춘다.
        if (자료.원고) {
          set원고({
            산출물: 자료.원고.산출물,
            시간: 자료.원고.시간,
            경고들: [...(자료.원고.구조체?.경고 ?? []), 자료.원고.생성?.warning].filter(
              Boolean,
            ) as string[],
          });
        }
      },
      () => {
        // 복원 실패는 알리지 않는다. 없는 것과 같이 다루고 새로 만들면 된다.
      },
    );
  }
```

기존 `배정고르기` 본문에 다른 처리가 있으면 지우지 말고 위 내용을 이어 붙인다.

뼈대와 원고를 만드는 자리에 셋을 실어 보낸다. 지금 코드는 이렇다.

```tsx
      const 자료 = await 뼈대만들기({
        배정: 선택배정,
        프로필,
        ...(공개강연인가 ? { 공개강연입력: 공개입력 } : {}),
      });
```

이렇게 바꾼다. 공개강연입력을 조건부로 얹는 부분은 그대로 둔다.

```tsx
      const 자료 = await 뼈대만들기({
        배정: 선택배정,
        프로필,
        ...(공개강연인가 ? { 공개강연입력: 공개입력 } : {}),
        주간: 고른주,
        배정번호: 배정들.indexOf(선택배정),
        배정제목: 선택배정.제목,
      });
```

원고 쪽은 지금 `const 자료 = await 원고만들기({ 뼈대, 프로필 });` 한 줄이다.
이렇게 바꾼다.

```tsx
      const 자료 = await 원고만들기({
        뼈대,
        프로필,
        주간: 고른주,
        배정번호: 배정들.indexOf(선택배정),
        배정제목: 선택배정.제목,
      });
```

두 자리 모두 `선택배정` 이 `null` 이 아님을 이미 위에서 확인한 뒤다. `tsc` 가
그렇지 않다고 하면 그 자리에 맞는 가드를 더한다.

설정을 못 받은 동안에는 프로필 폼을 그리지 않는다. 기본값을 그려 두면 형제가
그 위에 입력하시다가 서버 값이 도착해 덮어써진다.

```tsx
      {설정불러옴 && 프로필 ? (
        <SpeakerProfileForm 프로필={프로필} 변경={프로필고침} />
      ) : (
        <StatusBox>설정을 불러오는 중입니다.</StatusBox>
      )}
      {설정오류 && <StatusBox 경고 역할="alert">{설정오류}</StatusBox>}
```

`프로필` 이 `null` 인 동안 참조하는 자리가 남아 있으면 `tsc` 가 잡는다. 잡히는
대로 위 방식으로 막는다.

- [ ] **Step 5: 브라우저 저장을 지운다**

```bash
git rm web/src/lib/talk-profile-store.ts
```

지운 뒤 참조가 남았는지 확인한다.

```bash
grep -rn "talk-profile-store\|프로필읽기\|프로필쓰기" web/src src || echo "남은 참조 없음"
```

Expected: 남은 참조 없음.

- [ ] **Step 6: 확인한다**

```bash
npx tsc --noEmit
npm run smoke:web
npm test
rm -rf web/dist && npm run build:web
```

Expected: 전부 통과.

**스모크 출력에서 연설 화면에 "설정을 불러오는 중입니다." 가 보이는지 확인한다.**
서버 렌더는 이펙트를 돌리지 않으므로 설정이 없는 상태가 그려지는 것이 맞다.
프로필 폼이 그대로 보이면 `설정불러옴` 조건이 안 걸린 것이다.

- [ ] **Step 7: 커밋한다**

```bash
git add web/src/lib/use-weeks.ts web/src/lib/api.ts web/src/lib/talk-api.ts web/src/routes/Talk.tsx
git commit -m "설정을 서버에서 받고 저장된 연설을 복원한다"
```

---

### Task 9: 배포하고 실제로 확인

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 전체를 확인한다**

```bash
npm test
npx tsc --noEmit
rm -rf web/dist && npm run build:web
npm run smoke:web
```

Expected: 전부 통과.

- [ ] **Step 2: 로컬에서 손으로 확인한다**

```bash
node --env-file-if-exists=.env --no-warnings src/web-server.mjs
```

브라우저에서 `http://localhost:3000/` 을 열고 차례로 확인한다.

1. 파수대 연구에서 어느 주간을 준비한다. 진행률 게이지가 돌고 결과가 나온다
2. 다른 화면에 갔다가 돌아와 같은 주간을 준비한다. **게이지 없이 즉시** 나오고
   위에 "…에 만들어진 답변입니다" 띠가 보인다
3. 다시 만들기를 누른다. 확인 대화가 뜬다. 그만두면 아무 일도 없다
4. 다시 만들기를 확인한다. 새로 만들어지고 띠가 사라진다
5. 연설에서 배정을 고르고 뼈대를 만든다. 다른 화면에 갔다 돌아와 같은 배정을
   고르면 **뼈대가 그대로 있다**
6. 연설 프로필의 임명을 바꾸고 새로고침한다. 바뀐 값이 남아 있다

**되지 않은 항목은 보고서에 그대로 적는다.**

- [ ] **Step 3: README 에 저장 구조를 한 줄 적는다**

`README.md` 의 웹 관련 절에 이 문단을 더한다.

```markdown
## 자료 보관

주간 답변은 `weeks/{종류}/{주간}.json` 에 회중 공용으로 한 벌만 둔다. 한 번
만들면 모두가 그것을 본다. 다시 만들기를 누르면 모두의 것이 바뀐다.

연설 자료와 화자 설정은 `users/{회원번호}/` 아래에 사람별로 둔다. Vercel Blob
비공개 저장소를 쓰므로 함수를 거쳐야만 읽힌다.

설계는 `docs/superpowers/specs/2026-08-27-storage-and-mypage-design.md` 에 있다.
```

- [ ] **Step 4: 커밋하고 브랜치를 민다**

```bash
git add README.md
git commit -m "자료 보관 구조를 README 에 적는다"
git push -u origin storage
```

**머지하지 않는다.** 형제가 확인한 뒤 결정한다.

---

## 자체 검토

**설계 대비 빠진 것**

없다. 설계 10절의 1~5단계를 과제 1~9가 모두 덮는다.

- 3절 저장 구조 → 과제 1(경로), 2(주간), 4(설정), 5(연설)
- 4절 화면 동작 → 과제 7(안내 띠·다시 만들기), 8(연설 복원·설정)
- 5절 API → 과제 3, 4, 6
- 6절 코드 구조 → 과제 1, 2, 5 가 순수 모듈이고 3, 4, 6 이 얇은 어댑터다
- 7절 오류 처리 → 과제 2, 4, 5 의 시험이 여덟 줄을 모두 덮는다
- 8절 시험 → 과제 1, 2, 5 에 시험이 27개 있다

설계 11절의 마이페이지 화면은 2차이며 이 계획에 없다. 의도한 것이다.

**설계에 없던 결정 셋**

- **`보관` 을 자료 옆에 얹는다.** 자료를 한 겹 감싸면 화면의 모든 자리를 고쳐야
  한다. 필드 하나를 더하는 편이 변경 범위가 훨씬 작다
- **연설 저장에 `배정제목` 도 보낸다.** 설계 3절이 요구한 대조를 하려면 서버가
  제목을 알아야 한다
- **원고를 저장할 때 뼈대도 같이 넣는다.** 복원 한 번으로 두 단계가 다 살아나야
  화면을 나갔다 와도 이어서 작업할 수 있다

**이름 대조**

`주간열쇠`·`주간경로`·`설정경로`·`연설경로`(과제 1), `주간자료가져오기`(과제 2),
`설정읽기`·`설정쓰기`(과제 4), `연설읽기`·`연설쓰기`(과제 5),
`다시만들기`·`SavedNotice`(과제 7), `use설정`·`설정저장하기`·`보관된연설가져오기`
(과제 8). 뒤 과제가 부르는 이름이 앞 과제가 내보내는 이름과 모두 맞는다.

`저장소가져오기` 는 과제 3이 `src/web-server.mjs` 에 만들고 과제 6이 다시 쓴다.
과제 6은 새로 만들지 않는다.
