# 컨텍스트 노트

작업 중 내린 결정과 그 이유를 계속 덧붙인다. 최신 항목을 위에 둔다.

## 2026-07-31 — 절 목록을 범위로 접으려면 연속인지 먼저 봐야 한다

예습지의 성구 머리말이 주소가 둘 이상이면 무조건 "첫 절–마지막 절" 로 찍고 있었다.
`베드로 후서 3:7, 13` 이 `베드로 후서 3:7-13` 이 되고 `잠언 3:5, 6, 9` 가 `잠언 3:5-9`
가 된다. **본문은 정확하다 — 7절과 13절이 그대로 실린다. 틀린 것은 머리말이다.**
그런데 머리말이 곧 성구 주소이므로, 그것을 그대로 옮겨 쓰면 출판물이 인용하지 않은
범위를 인용하게 된다. CLAUDE.md 1번 규칙에 걸린다.

이번 주 기사에서는 드러나지 않았다. 쉼표 목록이 전부 인접 쌍(`19, 20` · `26, 27`)이라
접은 결과가 우연히 맞았기 때문이다. 시험도 못 잡았다 — 픽스처의 쉼표 목록 역시 전부
인접이었다. **실물 한 주치가 통과했다는 것이 그 코드가 옳다는 뜻이 아니다.**

이제 연속일 때만 접고 아니면 절 번호를 나열한다. 부수 효과로 `parseCitation` 이
나중에 장 넘는 범위를 허용하게 돼도 `6:37-1` 같은 오독이 생기지 않고, 역순 목록도
원 순서를 지킨다. 문단 번호 머리말에도 같은 결함이 있어 함께 고쳤다.

## 2026-07-31 — 인용 개수를 결산하지 않으면 파서가 조용히 얇아진다

이 브랜치에서 성구가 조용히 사라지는 결함이 네 번 나왔다. 다중 `data-rel-pid` 문단
유실, 속성 순서에 의존한 정규식, `href` 접두 의존, 주제 성구 둘째 이후 버림이다.
넷 다 예외를 던지지 않았다. 특히 `href` 건은 wol 이 절대 URL 로 바꾸는 것만으로
**모든** 인용이 탈락하는데, 질문 문단은 그대로 잡히므로 예습지가 질문만 있고 성구는
한 건도 없는 채로 종료 코드 0으로 정상 생성된다.

낱낱의 결함을 고치는 것만으로는 다음 것을 못 막는다. 그래서 결산을 붙였다.
`parseArticle` 이 문서 전체의 인용 앵커 수를 함께 돌려주고, 조립 스크립트가 그것을
예습지에 실린 인용 수와 대조해 모자라면 시끄럽게 알리고 종료 코드 1로 끝낸다.

비교 방향이 `<` 인 것이 중요하다. 다중 `data-rel-pid` 문단은 두 그룹에 붙으므로 같은
앵커가 실린 쪽에서 두 번 세인다. `!==` 로 하면 정상 상황에서 거짓 경보가 난다.
픽스처가 그 상황을 담고 있어서(실린 18 대 문서 17) 시험이 이것을 지킨다.

## 2026-07-31 — 기사 파서는 실물 마크업의 속성 순서를 믿지 않는다

계획이 준 `src/wol-article.mjs` 초안은 인용 앵커를 `href` → `data-bid` → `class` 순서로만
잡는 정규식을 썼다. 리뷰가 이것을 지적했다. 같은 저장소의 `src/wol-chapter.mjs` 는 이미
속성 순서에 의존하지 않게 짜여 있었다 — 실물 wol 마크업을 믿지 않기로 한 선례가 이미
있었는데 새 파서가 그것을 되돌린 셈이다. `class="parNum"` 과 `data-pnum` 도 같은 문제를
갖고 있었다.

또 하나, 초안은 `data-rel-pid="[49, 50]"` 처럼 한 문단이 두 질문 그룹에 걸릴 때 목록의
첫 pid 만 읽었다. 나머지 그룹에서는 그 문단의 문단 번호와 인용 성구가 통째로 사라진다.

두 결함의 공통점이 이 프로젝트에서 가장 위험한 형태라는 것이다. **예외를 던지지 않고
성구를 조용히 빠뜨린다.** 예습지에 성구가 하나 덜 실려도 사람이 알아채기 어렵다. 그래서
둘 다 고쳤다. 앵커와 스팬의 여는 태그를 통째로 잡은 뒤 그 안에서 속성을 각각 확인하는
방식으로 바꿨고, `class` 목록은 공백으로 쪼개 낱말 단위로 비교한다 — `b` 를 부분
문자열로 찾으면 `bl` 이나 `fn b2` 같은 각주 앵커까지 인용으로 오인하기 때문이다.

합성 픽스처에 세 가지 함정을 심어 뒀다. 두 그룹을 가리키는 `data-rel-pid`, `class` 가
`data-bid` 보다 앞에 오는 앵커, `data-pnum` 이 `class` 보다 앞에 오는 스팬이다. 고치기 전
코드로 이 시험들이 실제로 실패하는 것을 먼저 확인했다. 픽스처가 구현에 맞춰 쉬워지면
시험이 아무것도 지키지 못한다.

## 2026-07-31 — wol 성구 라벨은 권 이름을 생략할 수 있다

`시 101:6, 7; 119:63` 의 `119:63` 과 `고린도 전서 9:26, 27; 10:31` 의 `10:31` 은
링크 본문에 권 이름이 없다. 앞 참조에서 이어받아야 하는데, 이어받는 범위를 문서
전체로 잡으면 엉뚱한 권을 물어 온다. `data-bid` 가 `{그룹}-{그룹내순번}` 형식이고
같은 괄호 안의 참조가 같은 그룹 번호를 가지므로, **이어받기는 같은 bid 그룹 안에서만**
한다. 그룹의 첫 항목에 권 이름이 없으면 해석 실패로 처리한다.

권 이름 해소는 완전 일치 → 별칭표 → 접두 유일 일치 순이다. 후보가 둘 이상이면
조용히 고르지 않고 실패로 본다. 별칭표는 손으로 66개를 채우지 않고 규칙으로 안
풀리는 것만 넣는다. 새 약칭은 예습지의 "미해결" 로 드러나므로 그때 한 줄 더한다.

## 2026-07-30 — 배치 파일 본문에는 한국어를 못 쓴다

`성구.cmd` 에 한국어 `rem` 주석을 넣었더니 `'저장소' is not recognized as an internal or
external command` 오류가 섞여 나왔다. cmd.exe 는 배치 파일을 UTF-8 이 아니라 OEM 코드
페이지로 읽기 때문에, 한국어 바이트가 명령 구분자로 오해되어 그 조각을 실행하려 든다.
그래서 이 파일 하나만 "새 소스 파일 첫 줄은 한국어 주석" 규칙의 예외이며, 이유는 파일
안에 영문으로 적어 두고 `scripts/lookup.test.mjs` 가 본문이 ASCII 인지 감시한다.

파일 이름이 한국어인 것도 한계가 있다. 사람이 셸에서 `.\성구 마태복음 24:14` 로 치는 것은
되지만, 다른 프로그램이 `cmd.exe /c 성구.cmd` 로 대신 부르면 cmd 가 인자를 다시 해석하다
파일을 못 찾는다. 워크플로에서 성구 조회가 필요하면 배치 파일을 부르지 말고
`src/verse-address.mjs` · `src/bible-text.mjs` · `src/refs-lookup.mjs` 를 직접 import 한다.
65,578건을 매번 다시 읽지 않아 더 빠르기도 하다.

## 2026-07-30 — 줄바꿈은 .gitattributes 로 LF 에 고정한다

브랜치를 master 로 반지한 직후, 테스트 41개가 전부 통과하던 상태에서 파이프라인이 통째로
깨졌다. 이 기기의 `core.autocrlf` 가 시스템 범위에서 `true` 라, 체크아웃이 커밋된 LF 파일을
작업 트리에 CRLF 로 다시 만들었다. 그러면 모든 줄이 `\r` 로 끝나는데, **자바스크립트 정규식의
`.` 은 `\r` 을 매치하지 않는다.** 그래서 `^(\d+):(\d+)\t(.+)$` 가 31,194줄 전부에서 실패했다.

브랜치에서는 파일을 Node 가 쓴 그대로였고 git 이 다시 만든 적이 없어 드러나지 않았다.
테스트만으로는 못 잡는 종류였고 반지 단계가 잡았다. 지금은 `.gitattributes` 가 LF 를
강제하고, 줄을 나누는 코드는 `src/text-lines.mjs` 의 `splitLines` 로 `\r` 을 허용한다.
새 파일을 읽는 코드를 쓸 때 `content.split('\n')` 을 직접 쓰지 말고 `splitLines` 를 쓴다.

## 2026-07-30 — 참조 라벨 형식 결정 로직의 중복

`verify-refs.mjs` 와 `extract-refs.mjs` 는 참조 라벨 형식을 결정하는 로직을 각각 구현하고 있다. 두 파일의 복사본은 이미 어긋나 있다. `extract-refs.mjs` 는 범위의 시작 절 ID가 끝 절 ID보다 크면 예외를 던지는 역순 범위 가드를 갖고 있지만, `verify-refs.mjs` 의 복사본은 이 가드가 없다.

중복을 택한 이유는 `extract-refs.mjs` 를 import 하면 최상위 코드가 실행되어 `core/bible/refs/` 에 파일을 쓰기 때문이다. 현재는 역순 범위가 데이터베이스에 존재하지 않고, `extract-refs.mjs` 는 파일을 쓰기 전에 예외를 던지며, 검증자는 두 코드 복사본을 대조하지 않고 데이터베이스에서 새로 계산한 결과를 커밋된 파일과 비교하므로 대부분의 어긋남이 보고되는 불일치로 드러난다. 따라서 현재는 위험이 낮다.

권장 수정은 라벨 결정 로직(단일 절, 책 간 범위, 장 간 범위, 같은 장, 역순 범위 가드 포함)을 `src/verse-address.mjs` 에서 내보내는 순수 함수로 추출하는 것이다. 두 스크립트 모두 이미 안전하게 import 하는 파일이고 최상위 부작용이 없다. 이렇게 하면 두 스크립트가 같은 함수를 호출하고, 역순 범위 가드도 합성 쌍으로 단위 테스트할 수 있으며 `core/bible/refs/` 에 절대 쓰지 않는다.

## 2026-07-30 — 절 번호는 1로 시작하지 않을 수 있다

시편 116편은 절 번호 없는 표제(예: "다윗의 시가. 아들 압살롬을 피해 도망할 때")를 갖고 있고
wol 은 이를 0절로 매긴다. 요한복음 8장은 간음한 여자 대목(8:1-11)을 신세계역이 본문에서
빼고 각주로 처리해서 12절부터 시작한다. 그래서 `core/bible/index.json` 의 각 장 객체는
`firstVerseNumber` 와 `lastVerseNumber` 를 따로 갖는다. `verses` 는 행 수일 뿐이고 표제
행을 포함하므로, 절 번호를 순회할 때는 `1..verses` 가 아니라 반드시
`firstVerseNumber..lastVerseNumber` 를 써야 한다.

## 2026-07-30 — 절 라벨은 마지막 행만 믿을 수 있다

`BibleVerse.Label` 은 각 장 첫 행에서 절 번호 대신 장 번호를 표시 마커로 쓴다. 표제가
있는 116개 장은 둘째 행에도 시편 번호가 얹혀 있어 마찬가지로 못 믿는다. DB 전체
31,194행을 직접 대조한 결과, 라벨과 실제 절 번호가 어긋나는 위치는 정확히 1,122곳이며
장 첫 행(오프셋 0) 1,006곳과 표제 장 둘째 행(오프셋 1) 116곳으로 나뉜다
(1,006 + 116 = 1,122, 그 외 위치는 0건). **반대로 각 장의 마지막 행은 예외 없이 진짜
절 번호다.** 그래서 `firstVerseNumber` 는 항상 마지막 행에서
`마지막 행의 Label − (행수 − 1)` 로 역산해야 한다. 이것이 이 브랜치에서 가장 값어치가
큰 발견이다. 이를 모르고 첫 행이나 둘째 행의 라벨을 그대로 믿으면 시편 116편과 표제가
있는 장들의 절 번호가 통째로 어긋난다.

## 2026-07-30 — 상호 참조 범위가 장 경계를 넘을 수 있다

상호참조 65,578건 가운데 4건은 시작 절과 끝 절의 장이 다르다 — 열왕기상 9:10,
역대기상 13:5, 에스라 4:8, 다니엘 2:4 가 담고 있는 범위 참조다. 책이 달라지는 범위는
0건이다. 범위 라벨을 만들 때 끝 절이 시작 절과 다른 장이면 끝 절 앞에 장 번호를 반드시
붙여야 한다 — 안 붙이면 시작 절과 같은 장으로 오독된다 (예: "열왕기상 6:37-7:1" 을
"열왕기상 6:37-1" 로 잘못 표기하게 된다).

## 2026-07-29 — 성경 본문을 wol 에서 가져오기로 함

`nwtsty_KO.jwpub` 안의 `BibleVerse.Content` 는 AES 암호화되어 있고 키를 확보하지 못했다.
씨앗 후보 125개와 복호화 변형 5종을 조합해 625회를 시도했으나 전부 실패했고,
공개된 jwpub 문서에도 알고리즘이 없었다.

그래서 자료원을 둘로 나눴다. **구조는 DB, 본문은 wol** 이다.
DB 의 `BibleBook` · `BibleChapter` · `BibleCitation` 은 평문이라 그대로 쓸 수 있고,
wol 의 절 앵커(`v40-24-13-1`)와 DB 의 절 ID 가 같은 공간을 쓰기 때문에
두 자료원이 서로를 검증한다. 마태 24장이 양쪽 모두 51절로 확인됐다.

## 2026-07-29 — wol 은 로컬 Node 로만 접근한다

Claude 의 WebFetch 도구로는 wol.jw.org 이 두 번 다 60초 타임아웃이었다.
로컬 Node 프로세스의 `fetch` 는 HTTP 200 을 130ms 에 받는다.
따라서 wol 접근은 반드시 스크립트 안에서 한다.

## 2026-07-29 — 절 개수 기준선을 DB 에서 가져옴

통용되는 31,102절은 신세계역과 맞지 않는다. 신세계역은 후대 사본에만 있는 절을
본문에서 빼고 각주로 처리하기 때문이다. DB 기준 실제 값은 66권 1,189장 **31,194절** 이다.
검증은 이 값을 기준선으로 삼는다.

## 2026-08-07 — 웹앱은 서버 얇게, UI는 정적으로 시작한다

사용자가 `jw-assistant` 를 웹사이트로 만들고 싶다고 했다. 기존 승인 설계는 "웹 UI 없음" 이었지만 이번 요청이 더 최신이다. 다만 정확성 규칙과 코어 구조는 그대로 유지한다.

처음부터 React나 Next.js 같은 프레임워크를 넣지 않는다. 프로젝트 규칙에 외부 npm 의존성을 추가하지 않는다고 되어 있고, 현재 기능은 Node 24 기본 `http` 서버와 정적 HTML, CSS, JS로 충분하다. 추후 로그인과 서버 저장이 필요해지면 API 경계는 유지하고 서버 구현만 넓힌다.

답변 초안은 외부 AI API 없이 만든다. WOL 문단의 핵심 문장과 해당 문단의 인용 성구를 조합하고, 성구 본문은 반드시 `core/bible/text/` 에서 읽는다. 따라서 첫 버전은 "완성된 교리 해설" 보다 "근거가 붙은 답변 초안" 에 가깝다. 독자적인 새 해석을 만들지 않는 쪽을 우선한다.

생활과 봉사는 WOL 주간 집회 페이지의 `pub-mwb` 문서를 사용한다. 해당 문서에서 "영적 보물 찾기" 와 "회중 성서 연구" 주변의 질문을 추출한다. 현재 주의 회중 성서 연구 서책이 `용하` 라도 나중에 바뀔 수 있으므로 서책명을 고정하지 않고 섹션 제목과 질문 구조를 기준으로 잡는다.

## 2026-08-07 — WOL 웹앱 1차 구현 결과

웹앱은 `npm run web` 으로 `http://localhost:3000` 에서 실행한다. 정적 화면은 `web/` 에 있고 API는 `src/web-server.mjs` 가 제공한다. `/api/options` 는 오늘 기준 이전 10주와 이후 10주를 돌려준다. `/api/watchtower?date=YYYY-MM-DD` 는 파수대 연구 답변 초안을 돌려주고, `/api/life-ministry?date=YYYY-MM-DD` 는 생활과 봉사의 영적 보물 찾기와 회중 성서 연구 답변 초안을 돌려준다.

생활과 봉사의 회중 성서 연구는 WOL 교재의 보조 링크 `/pc/...` 가 응답하지 않아, 교재에 나타난 서책명과 장 번호를 출판물 색인으로 다시 해석한다. 현재 `용하` 는 WOL 출판물 심벌 `rr` 로 매핑했다. 나중에 서책이 바뀌면 `src/ministry-meeting.mjs` 의 `서책심벌` 에 매핑을 추가하거나, WOL에서 직접 심벌을 안정적으로 얻는 방법을 찾아야 한다.

성구 약칭은 파수대 기사보다 생활과 봉사 교재와 서책 본문에서 더 짧게 나온다. `렘 23:33` 같은 약칭과, 같은 `data-bid` 그룹 안에서 장 번호까지 생략된 `14, 15` 같은 조각을 해석하도록 `src/citation-parse.mjs` 를 보강했다. 이 변경은 현재 주 생활과 봉사 API에서 영적 보물 첫 질문이 성구 근거를 갖는지 확인해 검증했다.

검증 결과 `npm test` 는 133개 테스트 중 131개 통과, 2개 스킵이다. 로컬 서버에서 홈 HTML, 날짜 옵션 API, 파수대 API, 생활과 봉사 API를 확인했다. 현재 주 기준으로 파수대는 13개 답변, 생활과 봉사는 영적 보물 2개와 회중 성서 연구 20개 답변을 반환했다.

## 2026-08-07 — Vercel 배포는 로컬 서버와 Serverless 어댑터를 함께 둔다

로컬 개발은 `src/web-server.mjs` 를 계속 쓴다. Vercel은 장기 실행 HTTP 서버가 아니라 Serverless Function을 쓰므로 `api/options.js`, `api/watchtower.js`, `api/life-ministry.js` 를 얇은 어댑터로 추가했다. 세 함수는 기존 `src/prep-service.mjs` 를 그대로 호출하므로 준비 로직은 중복하지 않는다.

정적 화면은 `web/` 에 유지한다. `vercel.json` 에서 `/`, `/styles.css`, `/app.js` 를 `web/` 파일로 rewrite 한다. 이렇게 하면 로컬 서버 구조를 바꾸지 않고 Vercel에서도 같은 화면 경로를 유지할 수 있다.

WOL HTML 캐시는 로컬에서는 `.cache/wol`, Vercel에서는 `os.tmpdir()/jw-assistant-wol` 을 쓴다. 서버리스 파일 시스템에서는 배포 디렉터리가 쓰기 불가능할 수 있으므로, 캐시 쓰기가 실패해도 HTML 응답은 그대로 돌려주도록 했다.

첫 배포 뒤 `/api/watchtower` 와 `/api/life-ministry` 가 `/var/task/core/bible/index.json` 을 찾지 못했다. Vercel의 함수 번들 추적이 `fs` 로 읽는 데이터 파일을 자동 포함하지 않았기 때문이다. `vercel.json` 의 `functions["api/*.js"].includeFiles` 에 `core/bible/**` 를 추가해야 한다.

Vercel에서 성경 코어 파일을 포함한 뒤에는 WOL 페이지 요청이 `fetch failed` 로 실패했다. Node 기본 `fetch` 의 런타임 차이를 줄이기 위해 `src/wol-fetch.mjs` 에 IPv4 우선 DNS 설정, `Accept-Encoding: identity`, Node `https` 폴백, 실패 원인 요약을 추가했다.

최종 프로덕션 배포는 `https://jw-assistant-seven.vercel.app` 별칭으로 확인했다. 루트 페이지는 HTTP 200, `/api/options` 는 21주 옵션, `/api/watchtower?date=2026-08-03` 은 13개 답변, `/api/life-ministry?date=2026-08-03` 은 영적 보물 2개와 회중 성서 연구 20개 답변을 반환했다.

Vercel 프로젝트는 `honeskor-1168s-projects/jw-assistant` 로 만들어졌다. Vercel CLI가 GitHub 저장소 연결을 시도했지만, Vercel 계정에 GitHub Login Connection이 없어 자동 배포 연결은 완료되지 않았다. 현재 배포는 CLI 로컬 업로드 방식으로 완료됐고, 자동 배포를 켜려면 Vercel 계정 설정에서 GitHub Login Connection을 먼저 추가해야 한다.

## 2026-08-07 — `용하` 출판물 심벌과 장 링크 형식 오류

2026년 8월 3일 주간 생활과 봉사 교재는 회중 성서 연구 범위를 `「용하」 2장`으로 표시한다. 공식 자료를 직접 확인한 결과 `용하`는 「용기 있게 하느님과 함께 걸으십시오」이고 WOL 출판물 심벌은 `wcg`다. 2장은 노아를 다루는 「세상을 정죄하다」다.

기존 `src/ministry-meeting.mjs`는 `용하`를 `rr`로 잘못 매핑했다. `rr`는 「순결한 숭배—마침내 회복되다!」이므로 그 책의 2장을 가져와 카인과 아벨 내용이 화면에 표시됐다. 또한 `wcg` 출판물 색인의 장 링크는 기존 서책처럼 `2장`이 아니라 `2 노아`로 표시되므로, 심벌만 바꾸면 장 링크를 찾지 못한다. 장 탐색기는 두 형식을 모두 처리해야 한다.

조회한 공식 자료는 `https://wol.jw.org/ko/wol/d/r8/lp-ko/202026245`, `https://wol.jw.org/ko/wol/publication/r8/lp-ko/wcg`, `https://wol.jw.org/ko/wol/d/r8/lp-ko/1102025902`이며 조회일은 2026-08-07이다.

`wcg` 2장을 실제 준비 서비스에 넣자 `기사 구조를 찾을 수 없다 — 질문 문단이 하나도 없다` 오류가 추가로 드러났다. 기존 파수대와 `rr` 서책은 질문에 `class="qu"`를 쓰고 답변 문단을 `data-rel-pid`로 연결하지만, `wcg`는 질문 문단 바로 뒤에 `div.gen-field` 답 입력 칸을 둔다. 기존 파수대 구조를 우선 처리하고 질문 그룹이 없을 때만 `gen-field` 구조를 읽는 폴백이 필요하다.

폴백은 `gen-field` 직전 문단 13개를 각각 질문으로 뽑고, 질문 핵심어와 가장 많이 겹치는 장 본문을 먼저 배치한다. 이 순서는 외부 AI 없이 답변 초안을 만드는 현재 방식에서 첫 문장이 질문과 직접 관련되게 하기 위한 것이다. 기존 `qu` 질문 구조에는 적용하지 않는다.

수정 후 `npm test`는 135개 중 133개 통과, 2개 스킵, 실패 0개다. `prepareLifeAndMinistry('2026-08-03')`의 회중 성서 연구 출처는 `https://wol.jw.org/ko/wol/d/r8/lp-ko/1102025902`, 질문은 13개였다. 첫 질문은 `노아는 어떻게 용기를 나타냈습니까?`이고 첫 답변은 노아와 가족에게 용기가 필요했다는 문장으로 시작했다.

수정 커밋 `a6d5453`을 GitHub `origin/main`에 푸시하고 Vercel 프로덕션에 재배포했다. 배포 ID는 `dpl_873TDr9Kg2vGhKCG63ypCCm1mBix`이고 기존 별칭 `https://jw-assistant-seven.vercel.app`에 연결됐다. 외부 검증에서 홈은 HTTP 200이었고 생활과 봉사 API는 오류 없이 `1102025902`, 질문 13개, 노아의 용기에 관한 첫 답변을 반환했다.

## 2026-08-07 — 답변 단위와 AI 생성 개선 원칙

사용자는 반복되는 `문단의 흐름을 근거로 보면`, `성구 근거로는` 문구를 없애고 실제로 말할 수 있는 답변만 복사하기를 원한다. 성구 본문은 계속 별도 참고 영역에 표시하되 클립보드에는 `답변` 필드만 기록한다.

질문 안에 `(ㄱ)`과 `(ㄴ)`이 함께 있으면 화면에서 하나의 카드로 취급하지 않는다. 서버가 질문을 하위 문항별 답변 객체로 나누고 각 객체가 독립된 복사 버튼을 갖게 한다. WOL 대화형 서책의 `토의해 보십시오`, `더 깊이 알아보기`, `교훈점 적용하기`, `시야를 넓혀 생각해 보기` 같은 소제목도 파싱해 질문 앞에 표시한다.

추론과 삽화 이해가 필요한 질문은 서버에서 OpenAI Responses API로 생성한다. 외부 npm 패키지는 추가하지 않고 Node 기본 `fetch`를 사용하며 기본 모델은 이미지 입력과 구조화 출력을 지원하는 `gpt-5.4-mini`로 한다. `OPENAI_API_KEY`는 서버 환경 변수로만 읽고 브라우저에 노출하지 않는다. 키가 없거나 API 호출이 실패하면 WOL 문단과 로컬 성경 본문으로 만든 결정론적 답변을 반환해 서비스 자체는 계속 동작하게 한다.
