---
name: 집회준비
description: 그 주 파수대 연구 기사를 예습한다. 예습지를 만들고, 함께 보며 답을 잡고, 끝에 코어로의 승격을 제안한다.
---

# 집회준비

## 순서

1. **산출물이 이미 있는지 먼저 확인한다.** 경로는
   `activities/meetings/<주 시작일>/파수대-예습.md` 다. 이미 있으면 스크립트를
   다시 돌리지 않는다. 형제님이 "내 답" 칸에 이미 답을 적어 두었을 수 있으므로,
   그 파일을 읽어 이어서 진행할지 새로 만들지 형제님께 먼저 묻는다. 새로 만들기로
   확인받은 뒤에만 `npm run 집회준비 -- --덮어쓰기` 를 쓴다. 확인 없이 `--덮어쓰기`
   를 붙여 다시 돌리지 않는다.

2. **없으면 예습지를 만든다.** `npm run 집회준비` 를 돌린다. 다른 주를 원하면
   `npm run 집회준비 -- 2026-08-02` 처럼 날짜를 준다. wol 구조가 바뀌어 기사를
   못 찾으면 `--docid` 로 직접 지정한다. 전체 사용법은 `npm run 집회준비 -- --help`
   로 본다.

3. **종료 코드를 곧바로 실패로 읽지 않는다.** 미해결 인용이 하나라도 있으면
   예습지는 정상적으로 만들어진 뒤에도 종료 코드 1로 끝난다. 콘솔에 찍힌
   "인용 N건 중 M건 해석, K건 미해결" 줄과 그 아래 목록을 먼저 본다. (1단계에서
   산출물이 이미 있어 멈춘 경우도 종료 코드 1이지만, 이때는 파일이 새로 만들어지지
   않고 메시지도 다르다 — 둘을 혼동하지 않는다.)

4. **산출물을 읽는다.** 스크립트가 마지막에 알려 준 경로를 쓴다.

5. **미해결을 확인한다.** 문서 끝 "미해결" 절이 비어 있지 않으면 형제님께 알린다.
   해석하지 못한 성구가 있다는 뜻이므로 조용히 넘어가지 않는다.

6. **함께 본다.** 문단그룹을 순서대로 짚으며 질문에 답을 잡는다. 막히는 곳은
   인용 성구와 그 역참조를 함께 놓고 본다.

7. **승격을 제안한다.** 아래 규칙을 따른다.

## 승격 규칙

설계 문서 §5.2 다. 활동에서 얻은 것 중 다음에 해당하면 코어로 올릴지 **반드시 묻는다**.

- 특정 절에 대한 새로운 이해 → `core/verses/<권>-<장>-<절>.md`
- 여러 성구에 걸친 개념 정리 → `core/topics/<주제>.md`
- 히브리어·그리스어 단어의 의미 → `core/words/<단어>.md`

이미 노트가 있으면 새로 만들지 않고 덧붙이며 날짜를 남긴다. 노트끼리는 `[[링크]]` 로
잇는다. 승격한 것은 `profile/progress.md` 의 "승격 기록" 에도 한 줄 남긴다.

## 지켜야 할 것

- **성구 본문을 직접 옮겨 적지 않는다.** 예습지에 이미 코어에서 온 본문이 들어 있다.
  더 필요하면 `src/bible-text.mjs` 를 import 해서 읽는다. 기억으로 인용하지 않는다.
- **`성구.cmd` 를 부르지 않는다.** 파일 이름이 한국어라 프로그램이 부르면 깨진다.
  `src/verse-address.mjs` · `src/bible-text.mjs` · `src/refs-lookup.mjs` 를 직접 쓴다.
- **교리 설명에는 출판물 근거를 단다.** 근거를 찾지 못하면 "출판물 근거 미확인 —
  내 정리임" 이라고 명시한다.
- **독자적인 새 해석을 만들지 않는다.** 여호와의 증인의 이해를 기준으로 삼는다.
- **조회한 출판물은 URL 과 조회 날짜를 남긴다.**
- **한국어 문장은 마침표로 끝낸다.** 문장 끝에 콜론을 쓰지 않는다.
- **wol 접근은 스크립트 안에서 한다.** WebFetch 도구는 wol 에서 타임아웃 난다.
