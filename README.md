# jw-assistant

김동언 형제(35세, 광주양림회중)의 개인 성경 연구를 돕는 도구다.
신세계역 성경 66권의 본문과 상호참조를 로컬 파일로 구축하고 기계로 검증한다.
설계 문서는 `docs/superpowers/specs/2026-07-29-jw-assistant-design.md`,
구현 계획은 `docs/superpowers/plans/2026-07-29-bible-core.md` 에 있다.
현재 웹 서비스의 운영 구조, 장애 해결법, 유지보수 절차와 남은 위험은
`docs/핵심-운영-인수인계.md`를 정본으로 삼는다.

## 파이프라인

산출물은 아래 순서로만 만들어진다. 뒤 단계는 앞 단계의 산출물을 전제하므로 순서를
바꾸면 안 된다.

1. `npm run unpack` — 원본 jwpub 에서 SQLite DB 를 `.cache/nwtsty_KO.db` 로 꺼낸다.
2. `npm run extract:index` — DB 에서 권·장·절 인덱스를 뽑아 `core/bible/index.json` 을 만든다.
3. `npm run fetch:bible` — wol.jw.org 에서 본문을 장 단위로 받아 `core/bible/text/` 에 쓴다.
   1.5초 간격으로 1,189장을 받으므로 약 30분이 걸린다. 받은 원본 HTML 을 `.cache/wol/` 에
   캐시하므로, 중간에 끊겨도 이미 받은 장은 다시 받지 않고 안전하게 이어받을 수 있다.
4. `npm run verify` — 수집된 본문이 인덱스 기준선과 일치하는지 검사한다.
5. `npm run extract:refs` — DB 의 절-대-절 상호참조를 뽑아 `core/bible/refs/` 에 TSV 로 쓴다.
6. `npm run verify:refs` — 상호참조가 DB 재계산 값과 일치하는지 검사한다.

## 성구 조회

성구 하나 또는 범위를 주면 본문과 난외 참조를 양방향으로 펼쳐 준다.

```
성구 마태복음 24:14
성구 시편 3:0-3
성구 열왕기상 6:37-7:1
```

저장소 루트의 `성구.cmd` 가 진입점이다. 다른 디렉터리에서 전체 경로로 불러도 되고,
PowerShell 에서는 `.\성구 마태복음 24:14` 처럼 부른다. npm 으로도 같은 일을 한다.

```bash
npm run 성구 -- "마태복음 24:14"
```

`성구.cmd` 에는 두 가지 제약이 있다. 둘 다 cmd.exe 가 UTF-8 을 쓰지 않는 데서 온다.

**본문은 반드시 ASCII 로 둔다.** cmd.exe 는 배치 파일을 OEM 코드 페이지로 읽어서, 한국어
주석을 넣으면 그 바이트를 명령으로 실행하려 든다. 이 파일 하나만 한국어 주석 규칙의
예외이며, 테스트가 이를 감시한다.

**다른 프로그램이 `cmd.exe /c 성구.cmd` 로 대신 부를 수는 없다.** 파일 이름이 한국어라
cmd 가 인자를 다시 해석하면서 파일을 못 찾는다. 사람이 셸에서 직접 치는 경우는 문제없다.
나중에 워크플로가 성구 조회를 프로그램으로 써야 하면, 이 배치 파일을 부르지 말고
`src/verse-address.mjs` · `src/bible-text.mjs` · `src/refs-lookup.mjs` 를 직접 import 한다.
그 편이 65,578건을 매번 다시 읽지 않아 훨씬 빠르기도 하다.

출력은 세 부분이다. **본문**, **이 성구가 가리키는 참조**(각각 본문까지), 그리고 **이 성구를 가리키는 참조**다. 세 번째가 난외 참조에는 없는 정보다. 난외 참조는 한쪽 방향으로만 기록되어 있어서, 어느 성구들이 이곳을 가리키는지는 65,578건을 뒤집어야 알 수 있다.

권 이름은 공백을 넣어도 되고 빼도 된다. 시편 표제는 0절로 조회한다.
`lookup` 이라는 영문 별칭도 같은 일을 한다.

## 집회 준비

그 주 파수대 연구 기사의 질문과 인용 성구를 모아 예습지를 만든다. 기사 본문은
담지 않는다. 인용 성구만 `core/bible/text/` 의 본문으로 펼친다.

```bash
npm run 집회준비                     # 오늘이 속한 주
npm run 집회준비 -- 2026-08-02       # 날짜로 다른 주를 지정
npm run 집회준비 -- --docid 2026403  # 기사를 직접 지정
npm run 집회준비 -- --덮어쓰기       # 이미 있는 예습지를 덮어쓴다
```

산출물은 `activities/meetings/<주 시작일>/파수대-예습.md` 다. 받은 HTML 은
`.cache/wol/` 에 캐시하므로 다시 돌려도 wol 을 또 부르지 않는다.

예습지가 이미 있으면 "내 답" 칸에 적어 둔 것이 날아갈 수 있으므로 덮어쓰지 않고
멈춘다. 다시 만들려면 `--덮어쓰기` 를 명시적으로 붙여야 한다.

해석하지 못한 성구 라벨은 예습지에 `⚠` 로 남고 문서 끝 "미해결" 절에 모인다.
스크립트도 마지막에 개수를 보고한다. 조용히 건너뛰지 않는다.

## 웹앱

로그인 없이 생활과 봉사 및 파수대 연구 답변을 준비하는 웹 화면을 실행한다.

```bash
npm run web
```

기본 주소는 `http://localhost:3000`이다. 서버에서 AI 답변을 생성하려면
`.env.example`에 적힌 `OPENAI_API_KEY`를 실행 환경에 설정한다. 기본 모델은
`gpt-5.6-terra`이며 `OPENAI_MODEL`로 바꿀 수 있다. 추론 강도는 `high`이며
`OPENAI_REASONING_EFFORT`로 바꾼다. 값은 `none`·`minimal`·`low`·`medium`·`high`·`xhigh`·`max` 다. 비밀 키는 브라우저 코드나 Git에
넣지 않는다. 키가 없거나 API 호출이 실패하면 공식 자료와 로컬 성경 본문으로 만든
초안을 표시한다.

Vercel 프로덕션 주소는 `https://jw-assistant-seven.vercel.app`이다. 배포 환경에서는
Vercel 프로젝트의 `OPENAI_API_KEY` 환경 변수를 사용한다.

## 자료 보관

카카오로 로그인해야 쓸 수 있다. 만든 자료는 서버에 남으므로 화면을 나갔다 와도
그대로 있다.

주간 답변은 `weeks/{종류}/{주간}.json` 에 **회중 공용으로 한 벌만** 둔다. 날짜만
있으면 결과가 정해지므로 사람마다 따로 만들 이유가 없다. 한 번 만들면 모두가
그것을 보고, 다시 만들기를 누르면 모두의 것이 바뀐다. 그래서 확인을 한 번 묻는다.

연설 자료와 화자 설정은 `users/{회원번호}/` 아래에 사람별로 둔다. 뼈대와 원고는
만들어질 때마다 저장되므로 뼈대만 만들고 나가도 날아가지 않는다. 설정이 서버에
있으므로 폰과 컴퓨터가 같은 값을 본다.

Vercel Blob 비공개 저장소를 쓴다. 함수를 거쳐야만 읽히고, 브라우저가 보낸 글자가
파일 경로가 되는 자리는 없다. 화면은 주간과 배정 번호만 보내고 경로는 서버가 만든다.

설계는 `docs/superpowers/specs/2026-08-27-storage-and-mypage-design.md` 에 있다.
마이페이지 화면(보관함·설정 탭)은 아직 없다. 그 문서 11절에 무엇을 만들지 적어 두었다.

## 연설 준비

그 주 「그리스도인 생활과 봉사」 교재에서 연설 배정을 뽑아 원고를 만든다. 웹앱의
`연설` 화면이다.

지원하는 연설은 넷이다.

| 종류 | 자료 출처 | 자격 |
|---|---|---|
| 성경에 담긴 보물 연설 (10분) | 교재. 소제목까지 준다 | 장로와 봉사의 종 |
| 학생 과제 (시연·성경 낭독·학생 연설) | 교재 | 시연은 자매도 가능. 나머지는 형제 |
| 그리스도인 생활 부분 | 교재 | 장로와 봉사의 종 |
| 일요일 공개강연 | 형제가 직접 입력 | 장로와 봉사의 종 |

**공개강연 개요는 WOL 에 없다.** 회중에 배부되는 자료이기 때문이다. 그래서 제목과
주제 성구와 소제목을 화면에서 직접 입력한다. 입력한 문장은 고치지 않는다. 더하는 것은
예화와 청중 적용과 시간 배분뿐이다.

성별과 임명이 고를 수 있는 배정을 좁힌다. 못 하는 배정은 회색으로 두고 왜 막혔는지
함께 보여 준다. 조용히 숨기지 않는다.

산출물은 넷이며 각각 복사하고 내려받을 수 있다.

- **준비용 원고** — 배경, 지정 요점, 설정, 시간 배분, 원고 본문, 성구, 축약 순서, 출처
- **낭독용 대본** — 대사와 동작 지시만. 시연이면 보조자와 나눠 갖는다
- **연단 큐카드** — 구간과 요점과 성구 주소만. 본문은 없다
- **자기 점검표** — 지정 요점이 어느 대목에서 드러나는지

**성구 본문은 언제나 `core/bible/text/` 에서만 온다.** 모델은 성구 주소만 쓰고
`{{성구:권 장:절}}` 자리표시자를 남기며, 서버가 로컬 본문으로 치환한다. 모델이 성구
주소 가까이에 로컬 본문과 다른 문장을 인용하면 한 번 다시 요청하고, 그래도 어기면 그
칸을 비우고 경고를 남긴다. 산출물 끝에 신세계역 본문이 실린 곳이 몇 군데인지 명시한다.

낭독 시간은 글자 수로 계산한다. 모델에게 묻지 않는다. 분당 글자수 기본값은 320이며
화면에서 조절한다. **실제로 소리 내어 읽어 보고 맞추는 값이다.**

### 교본 매니페스트를 다시 만들 때

「읽고 가르치는 기술을 발전시키십시오」(「읽가」) 20과와 「사람들을 사랑하고 제자로
삼으십시오」(「랑제」) 12과의 과별 요점을 `src/teaching-lessons.json` 에 담아 둔다.
런타임은 이 파일만 읽고 네트워크를 쓰지 않는다.

```bash
npm run refresh:teaching-lessons
```

다시 돌려야 할 때는 두 경우다. 협회가 두 교본을 개정했을 때와, 교재가 인용하는 과가
매니페스트에 없을 때다. WOL 을 1.5초 간격으로 두드리므로 처음에는 몇 분 걸린다.
추출된 과 수가 기준선(「읽가」 20, 「랑제」 12)보다 적으면 파일을 쓰지 않고 종료 코드
1로 끝난다.

## Vercel 자동 배포 검증 기록

2026-08-09에 Vercel GitHub 앱을 `iam-kimleader/jw-assistant` 저장소에만 허용하고, Vercel 프로젝트의 Git 연결을 `main` 브랜치 자동 프로덕션 배포 검증용으로 다시 확인했다.
